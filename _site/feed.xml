<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="http://localhost:4000/MonthlyProjects/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/MonthlyProjects/" rel="alternate" type="text/html" /><updated>2025-09-28T21:24:08+02:00</updated><id>http://localhost:4000/MonthlyProjects/feed.xml</id><title type="html">Marcus Presutti</title><subtitle>List of projects by Marcus Presutti</subtitle><author><name>Marcus Presutti</name><email>marcus.presutti.eu@gmail.com</email></author><entry><title type="html">7. LiDAR sensor</title><link href="http://localhost:4000/MonthlyProjects/lidar" rel="alternate" type="text/html" title="7. LiDAR sensor" /><published>2025-09-01T00:00:00+02:00</published><updated>2025-09-01T00:00:00+02:00</updated><id>http://localhost:4000/MonthlyProjects/lidar</id><content type="html" xml:base="http://localhost:4000/MonthlyProjects/lidar"><![CDATA[<h2 id="prologue"><a href="#prologue"></a>Prologue</h2>
<p>The first time I heard about a LiDAR, was in the context of self-driving cars ~5 years ago. Apparently, this type of distance sensor is so advanced that it can easily map its surrounding with high speed. I have only worked with an ultra-violet distance sensor for my electronic car set, which did not show very promising results. I was fascinated to hear that there exists sensors out there that actually does accurate distance measuring.</p>

<p>I was completely astounded when I saw <a href="https://www.youtube.com/watch?v=IQJL3htsDyQ">this YouTube video</a> of Mark Rober, who managed to map the entire Disneyland Space Mountain ride using a LiDAR sensor. The layout of this rollercoster ride has been kept secret, as the ride is completely dark.</p>

<p>Fast forward to a month back, where a family member of mine pitches a fun project, that might have some real-life applications. Without getting into specifics, the task would be to detect the width of a hole in a wall, in a place that is less accesible to people - using a LiDAR for accurate measurements. Thus, I ordered the cheapest reliable LiDAR from the internet that I could find, and started working!</p>

<h2 id="getting-started"><a href="#getting-started"></a>Getting Started</h2>
<p>I ended up buying an <a href="https://www.digikey.dk/da/products/detail/dfrobot/DFR0315/7597150?gclsrc=aw.ds&amp;gad_source=1&amp;gad_campaignid=19553954322&amp;gbraid=0AAAAADrbLlh-CWZhX98IEsoe4Sz-lI9Ua&amp;gclid=CjwKCAjwuePGBhBZEiwAIGCVS8z_Su1dd4dWNB7r0SnpCIOWeb-n-KSgTPRTWCMmvk1Hk53EswkguRoC9A0QAvD_BwE">RPLIDAR A1 from SlamTec</a>, on a Danish website. 
<img src="/MonthlyProjects/assets/images/lidar/RPLIDAR.png" alt="lidar" title="RPLIDAR" /></p>

<p>The LiDAR comes with associated software for rendering, but I ignored that, and used the raw output for my script. With the huge assistant of <a href="https://github.com/Roboticia/RPLidar">open-source implementations</a>, I was easily able to do some data processing.</p>

<p>To specify a goal for this project, I want to be able to hoist the LiDAR into a bucket, and let it detect the width of a circle that I have carved out of it</p>

<p>&lt;insert image here &gt;</p>

<h2 id="circle-detection"><a href="#circle-detection"></a>Circle Detection</h2>
<p>The objective is two-fold. I need to detect the presence of the circular surroundings, then I need to determine if the circle if “broken up” by a hole in the wall.</p>

<p>A LiDAR works by continuously “shooting” out lasers that quickly determine the position of an object. It does this while rotating in a single plane. So, this LiDAR only works in the plane (2D). Every measurement is associated with: a distance, an angle, and a timestamp - one row in a table. In order to visualize the circle that would seemingly appear, is to show the after-image of the measurements (if doing this live), or in general, only show a window of X measurements, or maybe within a T (milli)second window instead.</p>

<p>Using the <code class="language-plaintext highlighter-rouge">circle-fit</code> python package, I can easily find the best least-squares fit of a circle for my window of points. I am also using the <strong>DBSCAN</strong> algorithm (<code class="language-plaintext highlighter-rouge">scikit-learn</code>) for ensuring that we only fixate on the largest “connected component” - essentially ignoring all kinds of noisy measurements</p>

<p>The following animation shows how the circle is fit continuously, while being slowly hoisted up and down a simple bucket:</p>

<p><img src="/MonthlyProjects/assets/images/lidar/circle_detection.gif" alt="circle_detection" title="circle_detection" /></p>

<h2 id="hole-detection"><a href="#hole-detection"></a>Hole Detection</h2>
<p>Now that I can easily detect the circular environment, I want to check for “holes” in the circle. As we will see here, circle detection itself is not thaaat important for measuring the size of the hole. The circle detection is mainly needed for centering the coordinates of the measurements, but it also serves as a nice visual to see a clean circle.</p>

<p>Given all the “window points”, we want to check for the gaps between points, measured in difference in angle. For this purpose, centering the points around (0,0) makes angle calculation much cleaner. If a gap is larger than 50 degress, it is ignored. The following example shows the continuous tracking of the hole:</p>

<p><img src="/MonthlyProjects/assets/images/lidar/hole_detection.gif" alt="hole_detection" title="hole_detection" /></p>

<p>You will quickly realize that the hole moves quite a lot; and that is simply because the hoisting setup is not that robust. This means that the LiDAR itself was rotating. And since all measurements are relative to the LiDAR as the frame of reference, the hole seems to jump quite a bit. Not only that, but as it tilts, the circular surrounding is not captured correctly either.</p>

<h2 id="everything-together"><a href="#everything-together"></a>Everything Together</h2>
<p>For all of this to really work, I would need a robust hoisting system, otherwise we get the errors mentioned above. The current setup looks like this:</p>

<p><img src="/MonthlyProjects/assets/images/lidar/setup2.png" alt="setup2" title="setup2" /></p>

<p>It is a kitchen roll with a string attached between itself and the LiDAR. By rotating the roll, the LiDAR is pulled straight up, as the string is fixated using my camera tripod. The LiDAR itself needs to be connected to a computer while this happens - hence the wire that goes into my desktop computer.</p>

<h2 id="conclusion-and-future-work"><a href="#conclusion-future-work"></a>Conclusion and Future Work</h2>
<p>I think this is a great start. This proof of concept demo has shown that it is definitely not impossible to get this thing to work more robustly. The biggest challenge has been to create a stable hoisting setup. Maybe strings are not the best way to move it, since it is susceptible to unwanted rotations.</p>

<p>It did not seem to be a problem that the LiDAR was connected to my computer, but it is definitely a bottleneck in real applications. It would make sense to connect it with a RaspberryPi instead, as that could easily be attached to the LiDAR.</p>]]></content><author><name>Marcus Presutti</name><email>marcus.presutti.eu@gmail.com</email></author><summary type="html"><![CDATA[I got myself a LiDAR sensor - Let's try it out!]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4000/MonthlyProjects/lidar/RPLIDAR.jpg" /><media:content medium="image" url="http://localhost:4000/MonthlyProjects/lidar/RPLIDAR.jpg" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">5. Danish National Championship in AI</title><link href="http://localhost:4000/MonthlyProjects/A-dmiai_2025" rel="alternate" type="text/html" title="5. Danish National Championship in AI" /><published>2025-08-01T00:00:00+02:00</published><updated>2025-08-01T00:00:00+02:00</updated><id>http://localhost:4000/MonthlyProjects/A-dmiai_2025</id><content type="html" xml:base="http://localhost:4000/MonthlyProjects/A-dmiai_2025"><![CDATA[<h2 id="prologue"><a href="#prologue"></a>Prologue</h2>
<p>What a journey it has been to compete at the <a href="https://dmiai.dk/">Danish National Championship in AI</a> (hosted by <a href="https://ambolt.io/">Ambolt</a>). Initially, this was meant as a small thing I could do with a few friends, but ended up as a serious competition with a larger team than expected, and an actual chance of becoming Danish champions. We, <strong>Team Seje Rejer</strong>, managed to get 3rd place, which we are all very proud of, even if we were convinced that 1st place was very possible to achieve as well. We won 5,000 DKK for sharing, and were invited for the <a href="https://d3aconference.dk/">D3A conference</a> to share our solutions alongside the other top 3 teams. It has truly been fun - and we have all learned a ton in the process. Meeting the other teams at D3A was also exciting and fun, as we got to share solutions, and got to make some new friends!</p>

<p><img src="/MonthlyProjects/assets/images/dmiai/d3a.jpg" alt="d3a" title="d3a" />
(<em>That’s me on the left side of the podium!</em>)</p>

<h2 id="the-competition"><a href="#competition"></a>The Competition</h2>
<p>The competition is a week long hackaton where your team is evaluated across 3 different challenges. How many points you get in a task depends on your performance <em>relative</em> to the other contestants. The points of the indiviuals challenges are based on the <a href="https://racingnews365.com/f1-points-system">Formula 1 point system</a>; 1. place gets 25 points, 2. place gets 18 points, 3. place gets 15 points, etc.</p>

<p>The challenges are formatted in a way, such that we can host an endpoint to expose our model thrugh an API. The API will then have inputs sent to it, for which there are constraints on how long it can take to return a valid answer.</p>

<p>Throughout the week, we are able to repeatedly test our models on the <em>validation leaderboard</em>, which signals to everyone how people are doing so far in the competition. Although it is definitely possible to <em>overfit</em> a solution to the validation leaderboard, it should be a good indication as to how well you are expected to perform on the final evaluation test, for which we only have 1 attempt!</p>

<h2 id="challenge-1---emergency-heathcare-rag"><a href="#challenge-1"></a>Challenge 1 - Emergency Heathcare RAG</h2>
<p>As if decided by fate, one of the challenges was to design a RAG system, which I just happended to have spent my previous 2 months working on. I had the main responsiblity for this task.</p>

<p>The task is as follows:</p>
<blockquote>
  <p>We have 115 topics in the realm of medicine, each topic has some associated documents that will work as our <em>reference</em> data. We are then given a statement which has been generated based on one of those exact documents, and our job is to tell from which <em>topic</em> this statement belongs to, as well as determining if the statement itself is <em>true</em> or <em>false</em></p>
</blockquote>

<p>We were limited to 24GB of VRAM during inference, and the inference time must not exceed 5 seconds.</p>

<h3 id="our-solution">Our solution</h3>
<p>The solution is similar to the previous work I have done with RAG (previous post), so I won’t go into too much detail.</p>

<p>So, we started by chunking the reference data into chunks of varying lengths and overlaps, and then they were all embedded. The first challenge was really to get a good chunking strategy, as well as finding the most powerful embedding model that fit the 24GB VRAM constraint. We ended up using a model from HuggingFace - <code class="language-plaintext highlighter-rouge">Qwen/Qwen3-Embedding-4B</code>. Then, using the FAISS index, we can compare an embedded version of the medicinal statement with the collection of reference embeddings to find the most similar document, and thereby the supposed topic.</p>

<p>And that, we fed the chunk text into an LLM with the following prompts, in order to determine whether the statement was true or false, using the reference data:</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">system_prompt</span> <span class="o">=</span> <span class="p">(</span>
    <span class="s">"You are a helpful fact-checking assistant."</span>
    <span class="s">"You will be given a reference document and a statement."</span>
    <span class="s">"Respond only with 'TRUE' if the statement is supported by the reference, otherwise respond 'FALSE'."</span>
    <span class="s">"Do not guess. You MAY use outside knowledge to determine the reliability of the statement."</span>
<span class="p">)</span>
</code></pre></div></div>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">user_prompt</span> <span class="o">=</span> <span class="p">(</span>
    <span class="s">"Reference:"</span>
    <span class="sa">f</span><span class="s">"</span><span class="si">{</span><span class="n">reference</span><span class="si">}</span><span class="s">"</span>
    <span class="s">"Statement:"</span>
    <span class="sa">f</span><span class="s">"</span><span class="si">{</span><span class="n">statement</span><span class="si">}</span><span class="s">"</span>
    <span class="s">"Answer:"</span>
<span class="p">)</span>
</code></pre></div></div>
<p>As for the LLM we used the <code class="language-plaintext highlighter-rouge">Qwen/Qwen3-4B</code> model.</p>

<h3 id="others-solutions">Others’ solutions</h3>
<p>The interesting thing is that all the top teams had somewhat different strategies, but all managed to score ~93% on the validation leaderboard. I would argue that this must be a limit defined by the uncertainty of the validation dataset. In other words, we either have no way to tell which model is actually best, OR all the models are equally good.</p>

<p>The most major strategies that have deviated most from our solution was:</p>
<ol>
  <li>The use of the <strong>BM25 algorithm</strong> which is a bag-of-words (algorithmic, not deep learning) approach to the matching and retrieval</li>
  <li>Using <strong>only smaller chunks</strong>, such that stronger LLMs can be used. (<em>This is because more VRAM is required when the prompt size increases</em>)</li>
  <li>Using a <strong><em>reasoning model</em></strong> for “truth validation”, but manually cuts it off if its reasoning is too slow, and use a simple BERT type model in those cases</li>
</ol>

<p>I am confident that utilizing smaller chunks, and therefore a smarter LLM, would definitely have gotten us to the 91% evaluation accuracy along the other groups. Even if most top groups scored ~93%, ours were the only team that did not score 91% on the evaluation set, but instead got 89%.</p>

<p>I was not very much involved in the other 2 challenges. However, I did help with cloud hosting.</p>

<h2 id="challenge-2---racecar"><a href="#challenge-2"></a>Challenge 2 - Racecar</h2>
<p>The racecar challenge was a <strong>pygame</strong> game/environment where you had to control a car through a five lane highway. In each lane, a car can spawn with a speed relatively close to the speed of the player’s car. All top teams quickly reached the same conclusion - that reinforcement learning was not really viable, and it was better to code an expert system manually. The main challenge was really, that the car could only perceive its surroundings through 16 sensors that output the distance to the closest obstacle.</p>

<p><img src="/MonthlyProjects/assets/images/dmiai/race_car_intro.png" alt="race_car_intro" title="race_car_intro" /></p>

<p>With or without machine learning, it is a very hard task to consistently navigate through the track, because it’s hard to perceive cars in the adjacent lanes.</p>

<h2 id="challenge-3---tumor-segmentation"><a href="#challenge-3"></a>Challenge 3 - Tumor Segmentation</h2>
<p>This task was actually being recycled from a previous year, because of some trouble with the creators of the task we were suppsoed to have. We were supposed to work with diseases and eye scan image data.</p>

<p>The challenge now is to segment tumors from PET-scans. We knew from that other year, that the winning team had utilized the public <strong>nnUnet</strong> model, which we conveniently used as well - and so did many others. Despite that, we still managed to score higher than the others. Luck could have been involved, but we actually did modify the default implementation a bit:</p>
<ol>
  <li>We trained using specific image augmentations that matched the patterns seen in the validation dataset: {salt/pepper noise, horizontal flips, and small pertubations in angle}</li>
  <li>We created an ensemble of 5 models, where each model was trained on seperate 5 folds.</li>
</ol>

<h2 id="final-words"><a href="#final-words"></a>Final words</h2>
<p>Just for fun, here is the final evaluation leaderboard:</p>

<p><img src="/MonthlyProjects/assets/images/dmiai/eval_leaderboard.png" alt="eval_leaderboard" title="eval_leaderboard" /></p>

<p><a href="https://github.com/Marcrulo/TEAM_SEJE_REJER">As well as the code repository</a></p>]]></content><author><name>Marcus Presutti</name><email>marcus.presutti.eu@gmail.com</email></author><summary type="html"><![CDATA[An overview of how our team tackled the 3 tasks from DMiAI and came in 3rd 🏆🥉]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4000/MonthlyProjects/dmiai/d3a.jpg" /><media:content medium="image" url="http://localhost:4000/MonthlyProjects/dmiai/d3a.jpg" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">6. DTU course discovery</title><link href="http://localhost:4000/MonthlyProjects/B-dtu_courses" rel="alternate" type="text/html" title="6. DTU course discovery" /><published>2025-08-01T00:00:00+02:00</published><updated>2025-08-01T00:00:00+02:00</updated><id>http://localhost:4000/MonthlyProjects/B-dtu_courses</id><content type="html" xml:base="http://localhost:4000/MonthlyProjects/B-dtu_courses"><![CDATA[<h2 id="prologue"><a href="#prologue"></a>Prologue</h2>
<p>What started out as a curiosity, turned into an exam project in the DTU course “<strong><em>Social Graphs and Interactions</em></strong>” (02805). Repo can be found <a href="https://github.com/ThereseStein/CourseAnalyzer">here</a>. Even though we managed to create and analyse the course graph for the exam (<em>there was more depth to the project, but let’s omit that for now</em>), I knew that I wanted to somehow help other students in practice, using this data.</p>

<p><img src="/MonthlyProjects/assets/images/courses/big_graph.png" alt="big_graph" title="big_graph" /></p>

<p><em>The graph shows all courses (which have neighbors), and how they are connected to other courses by their prerequisites, where each node is colored by department, and has size equivalent to its degree.</em></p>

<p>Therefore I came up with a way to help fellow students, using a <a href="https://chromewebstore.google.com/detail/dtu-extended-course-overv/pfgokeibjgebafnamhbgkgfgbmhmgpde">Chrome extension that alters the DTU course website</a> (and the GitHub <a href="https://github.com/Marcrulo/DTU-courses-extension">here</a>). A problem with the current <a href="https://kurser.dtu.dk/course/02285">website</a> is, that while it does show what courses are required for taking ‘this’ course (the “prerequisites”), it does not show which courses it is a prerequisite to. In other words, it does not show the “subsequent” courses. In general is it quite hard to get an overview of how courses are connected… but the extension solves that!</p>

<p>I’d also like to add that the Javascript (JS) / Chrome extension part is heavily vibe-coded. It was very fascinating to orchestrate this and let the chatbot build the right features one at a time. All the JS logic is contained in a single file, so it’s easy to edit the entire thing at the same time. I realized that designing a good experience was very important, and has actually been quite challenging. As the AI’s “manager” I could focus more on the design and user experience, instead of being too fixated on the code itself, as I might become too attached to it, and less willing to change it.</p>

<p>Anyway, let’s go through the steps on by one, to see how it all comes together</p>

<h2 id="scraping"><a href="#scraping"></a>Scraping</h2>
<p>There is no API for retrieving course data nicely, which is why we need to scrape the course website instead (for each course). Using the <strong>BeautifulSoup4</strong> library we can easily go through all possible course number, and check if the website returns a valid course description:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Imports
</span><span class="kn">import</span> <span class="nn">requests</span>
<span class="kn">import</span> <span class="nn">json</span>
<span class="kn">from</span> <span class="nn">bs4</span> <span class="kn">import</span> <span class="n">BeautifulSoup</span>

<span class="c1"># Initial
</span><span class="n">base_url</span> <span class="o">=</span> <span class="s">"https://kurser.dtu.dk/course/"</span>
<span class="n">cookies</span> <span class="o">=</span> <span class="p">{</span>
    <span class="s">'ASP.NET_SessionId'</span> <span class="p">:</span> <span class="s">"&lt;SessionId&gt;"</span><span class="p">,</span>   <span class="c1"># not that important
</span>    <span class="s">'{DTUCoursesPublicLanguage}'</span> <span class="p">:</span> <span class="s">'en-GB'</span> <span class="c1"># ensure that the site is always in English
</span><span class="p">}</span>

<span class="c1"># Department number-to-name mapping
</span><span class="k">with</span> <span class="nb">open</span><span class="p">(</span><span class="s">'department_names.json'</span><span class="p">,</span> <span class="s">'r'</span><span class="p">)</span> <span class="k">as</span> <span class="nb">file</span><span class="p">:</span>
    <span class="n">departments</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="n">load</span><span class="p">(</span><span class="nb">file</span><span class="p">)</span>
<span class="n">departments_keys</span> <span class="o">=</span> <span class="nb">list</span><span class="p">(</span><span class="n">departments</span><span class="p">.</span><span class="n">keys</span><span class="p">())</span>

<span class="c1"># Loop through all possible course sites
</span><span class="n">valid_courses</span> <span class="o">=</span> <span class="p">{}</span>
<span class="k">for</span> <span class="n">dep</span> <span class="ow">in</span> <span class="n">departments_keys</span><span class="p">:</span>
    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span><span class="mi">1000</span><span class="p">):</span>
        <span class="n">course_num</span> <span class="o">=</span> <span class="sa">f</span><span class="s">"</span><span class="si">{</span><span class="n">dep</span><span class="si">}{</span><span class="n">i</span><span class="si">:</span><span class="mi">03</span><span class="si">}</span><span class="s">"</span>
        <span class="n">course_url</span> <span class="o">=</span> <span class="n">base_url</span> <span class="o">+</span> <span class="n">course_num</span>
        <span class="n">response</span> <span class="o">=</span> <span class="n">requests</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="n">course_url</span><span class="p">,</span> <span class="n">cookies</span><span class="o">=</span><span class="n">cookies</span><span class="p">)</span>
        <span class="n">soup</span> <span class="o">=</span> <span class="n">BeautifulSoup</span><span class="p">(</span><span class="n">response</span><span class="p">.</span><span class="n">content</span><span class="p">,</span> <span class="s">'html.parser'</span><span class="p">)</span>
        <span class="n">title</span> <span class="o">=</span> <span class="n">soup</span><span class="p">.</span><span class="n">title</span><span class="p">.</span><span class="n">string</span>
        <span class="k">if</span> <span class="n">title</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
            <span class="k">continue</span>
        <span class="n">valid_courses</span><span class="p">[</span><span class="n">course_num</span><span class="p">]</span> <span class="o">=</span> <span class="n">response</span><span class="p">.</span><span class="n">text</span>  

<span class="c1"># Save text content for each course description
</span><span class="k">with</span> <span class="nb">open</span><span class="p">(</span><span class="s">'valid_courses.json'</span><span class="p">,</span> <span class="s">'w'</span><span class="p">)</span> <span class="k">as</span> <span class="nb">file</span><span class="p">:</span>
    <span class="n">json</span><span class="p">.</span><span class="n">dump</span><span class="p">(</span><span class="n">valid_courses</span><span class="p">,</span> <span class="nb">file</span><span class="p">)</span>

</code></pre></div></div>

<h2 id="course-graphs"><a href="#graphs"></a>Course Graphs</h2>
<p>Now we need to extract the relevant information from the HTML pages in order to create a graph, using the <strong>NetworkX</strong> library.</p>

<p>We start by creating all the nodes:</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Initialize directd graph
</span><span class="n">G</span> <span class="o">=</span> <span class="n">nx</span><span class="p">.</span><span class="n">DiGraph</span><span class="p">()</span>

<span class="c1"># Go through each course
</span><span class="k">for</span> <span class="n">course_num</span> <span class="ow">in</span> <span class="n">valid_courses</span><span class="p">:</span>
    <span class="n">department</span> <span class="o">=</span> <span class="n">course_num</span><span class="p">[:</span><span class="mi">2</span><span class="p">]</span> 
    <span class="n">G</span><span class="p">.</span><span class="n">add_node</span><span class="p">(</span><span class="n">course_num</span><span class="p">,</span>
               <span class="n">course_num</span>      <span class="o">=</span> <span class="n">course_num</span><span class="p">,</span>
               <span class="n">page</span>            <span class="o">=</span> <span class="n">valid_courses</span><span class="p">[</span><span class="n">course_num</span><span class="p">],</span>
               <span class="n">department</span>      <span class="o">=</span> <span class="n">department</span><span class="p">,</span> 
               <span class="n">color</span>           <span class="o">=</span> <span class="n">department_colors</span><span class="p">[</span><span class="n">department</span><span class="p">],</span>
               <span class="n">department_name</span> <span class="o">=</span> <span class="n">department_names</span><span class="p">[</span><span class="n">department</span><span class="p">])</span>
</code></pre></div></div>

<p>Then we add directed edge that points <em>away</em> from the preresuisite course:</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">course_num</span> <span class="ow">in</span> <span class="n">valid_courses</span><span class="p">:</span>

    <span class="c1"># Initialize BeautifulSoup object
</span>    <span class="n">page</span> <span class="o">=</span> <span class="n">G</span><span class="p">.</span><span class="n">nodes</span><span class="p">[</span><span class="n">course_num</span><span class="p">][</span><span class="s">'page'</span><span class="p">]</span>
    <span class="n">soup</span> <span class="o">=</span> <span class="n">BeautifulSoup</span><span class="p">(</span><span class="n">page</span><span class="p">,</span> <span class="s">'html.parser'</span><span class="p">)</span>

    <span class="c1"># Define the search pattern to match both "Academic prerequisites" and "Mandatory prerequisites"
</span>    <span class="n">search_pattern</span> <span class="o">=</span> <span class="sa">r</span><span class="s">"(Academic prerequisites|Mandatory Prerequisites)"</span>
    
    <span class="c1"># Find the label element that matches the pattern
</span>    <span class="n">label</span> <span class="o">=</span> <span class="n">soup</span><span class="p">.</span><span class="n">find</span><span class="p">(</span><span class="s">'label'</span><span class="p">,</span> <span class="n">string</span><span class="o">=</span><span class="n">re</span><span class="p">.</span><span class="nb">compile</span><span class="p">(</span><span class="n">search_pattern</span><span class="p">))</span>
    <span class="k">if</span> <span class="n">label</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
        <span class="k">continue</span>  <span class="c1"># Skip if no label is found
</span>    
    <span class="c1"># Get the parent element that contains the label and prerequisites
</span>    <span class="n">parent</span> <span class="o">=</span> <span class="n">label</span><span class="p">.</span><span class="n">find_parent</span><span class="p">().</span><span class="n">find_parent</span><span class="p">()</span>
    
    <span class="c1"># Get the second &lt;td&gt; (assuming it contains the prerequisites text)
</span>    <span class="n">prerequisite</span> <span class="o">=</span> <span class="n">parent</span><span class="p">.</span><span class="n">find_all</span><span class="p">(</span><span class="s">'td'</span><span class="p">)[</span><span class="mi">1</span><span class="p">].</span><span class="n">text</span>

    <span class="c1"># Remove whitespace and line breaks
</span>    <span class="n">prerequisite</span> <span class="o">=</span> <span class="n">prerequisite</span><span class="p">.</span><span class="n">replace</span><span class="p">(</span><span class="s">'</span><span class="se">\r</span><span class="s">'</span><span class="p">,</span> <span class="s">' '</span><span class="p">).</span><span class="n">replace</span><span class="p">(</span><span class="s">'</span><span class="se">\n</span><span class="s">'</span><span class="p">,</span> <span class="s">' '</span><span class="p">)</span>

    <span class="c1"># Extract 5-digit course numbers
</span>    <span class="n">prerequisites</span> <span class="o">=</span> <span class="nb">set</span><span class="p">(</span><span class="n">re</span><span class="p">.</span><span class="n">findall</span><span class="p">(</span><span class="sa">r</span><span class="s">'\d{5}'</span><span class="p">,</span> <span class="n">prerequisite</span><span class="p">))</span>
    
    <span class="c1"># Add edges to the graph for valid prerequisites
</span>    <span class="k">for</span> <span class="n">prerequisite</span> <span class="ow">in</span> <span class="n">prerequisites</span><span class="p">:</span>
        <span class="k">if</span> <span class="n">prerequisite</span> <span class="ow">in</span> <span class="n">G</span><span class="p">.</span><span class="n">nodes</span><span class="p">:</span>
            <span class="k">if</span> <span class="n">prerequisite</span> <span class="o">!=</span> <span class="n">course_num</span><span class="p">:</span>  <span class="c1"># Skip self-loops
</span>                <span class="n">G</span><span class="p">.</span><span class="n">add_edge</span><span class="p">(</span><span class="n">prerequisite</span><span class="p">,</span> <span class="n">course_num</span><span class="p">)</span>
</code></pre></div></div>

<p>And the most important step is then to create a subgraph for each course number, where the course of interest is the center or root, and each layer/level “forward” is the next level of subsequent courses, and each layer/level “backwards” is the next level of prerequisite courses.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Get all courses
</span><span class="n">all_course_nums</span> <span class="o">=</span> <span class="nb">list</span><span class="p">(</span><span class="n">G</span><span class="p">.</span><span class="n">nodes</span><span class="p">())</span>

<span class="c1"># Create subgraph for all course numbers
</span><span class="k">for</span> <span class="n">center_node</span> <span class="ow">in</span> <span class="n">all_course_nums</span><span class="p">:</span>

    <span class="c1"># Get forward and reverse BFS nodes
</span>    <span class="n">forward_nodes</span> <span class="o">=</span> <span class="n">nx</span><span class="p">.</span><span class="n">single_source_shortest_path_length</span><span class="p">(</span><span class="n">G</span><span class="p">,</span> <span class="n">center_node</span><span class="p">)</span>
    <span class="n">reverse_nodes</span> <span class="o">=</span> <span class="n">nx</span><span class="p">.</span><span class="n">single_source_shortest_path_length</span><span class="p">(</span><span class="n">G</span><span class="p">.</span><span class="n">reverse</span><span class="p">(</span><span class="n">copy</span><span class="o">=</span><span class="bp">False</span><span class="p">),</span> <span class="n">center_node</span><span class="p">)</span>

    <span class="c1"># Combine all relevant nodes
</span>    <span class="n">all_nodes</span> <span class="o">=</span> <span class="nb">set</span><span class="p">(</span><span class="n">forward_nodes</span><span class="p">.</span><span class="n">keys</span><span class="p">())</span> <span class="o">|</span> <span class="nb">set</span><span class="p">(</span><span class="n">reverse_nodes</span><span class="p">.</span><span class="n">keys</span><span class="p">())</span>
    <span class="n">levels</span> <span class="o">=</span> <span class="p">{}</span>

    <span class="k">for</span> <span class="n">node</span> <span class="ow">in</span> <span class="n">all_nodes</span><span class="p">:</span>
        <span class="k">if</span> <span class="n">node</span> <span class="o">==</span> <span class="n">center_node</span><span class="p">:</span>
            <span class="n">levels</span><span class="p">[</span><span class="n">node</span><span class="p">]</span> <span class="o">=</span> <span class="mi">0</span>
        <span class="k">elif</span> <span class="n">node</span> <span class="ow">in</span> <span class="n">reverse_nodes</span><span class="p">:</span>
            <span class="n">levels</span><span class="p">[</span><span class="n">node</span><span class="p">]</span> <span class="o">=</span> <span class="o">-</span><span class="n">reverse_nodes</span><span class="p">[</span><span class="n">node</span><span class="p">]</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="n">levels</span><span class="p">[</span><span class="n">node</span><span class="p">]</span> <span class="o">=</span> <span class="n">forward_nodes</span><span class="p">[</span><span class="n">node</span><span class="p">]</span>

    <span class="c1"># Create a new DiGraph with only those edges
</span>    <span class="n">filtered_subG</span> <span class="o">=</span> <span class="n">nx</span><span class="p">.</span><span class="n">DiGraph</span><span class="p">()</span>
    <span class="n">filtered_subG</span><span class="p">.</span><span class="n">add_nodes_from</span><span class="p">(</span><span class="n">all_nodes</span><span class="p">)</span>
    

    <span class="c1"># Build layout positions
</span>    <span class="n">pos</span> <span class="o">=</span> <span class="p">{}</span>
    <span class="n">level_nodes</span> <span class="o">=</span> <span class="p">{}</span>
    <span class="k">for</span> <span class="n">node</span><span class="p">,</span> <span class="n">level</span> <span class="ow">in</span> <span class="n">levels</span><span class="p">.</span><span class="n">items</span><span class="p">():</span>
        <span class="n">level_nodes</span><span class="p">.</span><span class="n">setdefault</span><span class="p">(</span><span class="n">level</span><span class="p">,</span> <span class="p">[]).</span><span class="n">append</span><span class="p">(</span><span class="n">node</span><span class="p">)</span>

    <span class="k">for</span> <span class="n">level</span> <span class="ow">in</span> <span class="nb">sorted</span><span class="p">(</span><span class="n">level_nodes</span><span class="p">):</span>
        <span class="n">nodes</span> <span class="o">=</span> <span class="n">level_nodes</span><span class="p">[</span><span class="n">level</span><span class="p">]</span>
        <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">node</span> <span class="ow">in</span> <span class="nb">enumerate</span><span class="p">(</span><span class="n">nodes</span><span class="p">):</span>
            <span class="n">pos</span><span class="p">[</span><span class="n">node</span><span class="p">]</span> <span class="o">=</span> <span class="p">(</span><span class="n">level</span><span class="p">,</span> <span class="o">-</span><span class="n">i</span><span class="p">)</span>

    <span class="c1"># Remove edges if they don't point "forward"
</span>    <span class="n">edges</span> <span class="o">=</span> <span class="p">[</span>
        <span class="p">(</span><span class="n">u</span><span class="p">,</span> <span class="n">v</span><span class="p">)</span> <span class="k">for</span> <span class="n">u</span><span class="p">,</span> <span class="n">v</span> <span class="ow">in</span> <span class="n">G</span><span class="p">.</span><span class="n">subgraph</span><span class="p">(</span><span class="n">all_nodes</span><span class="p">).</span><span class="n">edges</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">pos</span><span class="p">[</span><span class="n">u</span><span class="p">][</span><span class="mi">0</span><span class="p">]</span> <span class="o">&lt;</span> <span class="n">pos</span><span class="p">[</span><span class="n">v</span><span class="p">][</span><span class="mi">0</span><span class="p">]</span>
    <span class="p">]</span>
    <span class="n">filtered_subG</span><span class="p">.</span><span class="n">add_edges_from</span><span class="p">(</span><span class="n">edges</span><span class="p">)</span>

    <span class="c1"># Write to JSON
</span>    <span class="n">graph_to_json</span><span class="p">(</span><span class="n">center_node</span><span class="p">,</span> <span class="n">filtered_subG</span><span class="p">,</span> <span class="n">levels</span><span class="p">)</span>
</code></pre></div></div>
<p>(<em>the rest of the code can be found in the <a href="https://github.com/Marcrulo/DTU-courses-extension">GitHub repo</a></em>)</p>

<p>To see what this looks like, consider the image below. The center is <strong>02180</strong>. Is has 5 prerequisites: (<strong>02100</strong>,<strong>02312</strong>,<strong>02105</strong>,<strong>01019</strong>,<strong>01017</strong>), and is itself the prerequisite to 3 courses (<strong>02256</strong>,<strong>02287</strong>,<strong>02285</strong>)</p>

<p><img src="/MonthlyProjects/assets/images/courses/layer_graph.png" alt="layer_graph" title="layer_graph" /></p>

<p>The goal is to insert this graph into the course website itself, and this is where we get to the <strong><em>Chrome Extension</em></strong></p>

<h2 id="chrome-extension"><a href="#chrome-extension"></a>Chrome Extension</h2>
<p>I quickly knew that it would be a problem to simply paste the image of the graph into the website. Not only is an image not interactive, but it would require quite a lot of data storage and loading.</p>

<p>Instead, I realized that the layered graph almost looked like a table structure, which is one of the most basic elements in HTML. Columns would indicate the “level” and rows would be added based on the number of nodes within the levels altogether.</p>

<h3 id="practical-stuff">Practical Stuff</h3>
<p>To start creating a Chrome extension, I simply followed <a href="https://developer.chrome.com/docs/extensions/get-started/tutorial/hello-world">this tutorial</a>. In essence, I the center of the project, <code class="language-plaintext highlighter-rouge">manifest.json</code></p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"DTU - Extended Course Overview"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">""</span><span class="p">,</span><span class="w">
  </span><span class="nl">"version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"1.0"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"manifest_version"</span><span class="p">:</span><span class="w"> </span><span class="mi">3</span><span class="p">,</span><span class="w">
  </span><span class="nl">"action"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"default_popup"</span><span class="p">:</span><span class="w"> </span><span class="s2">"popup.html"</span><span class="w">
  </span><span class="p">},</span><span class="w">
   </span><span class="nl">"content_scripts"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="p">{</span><span class="w">
        </span><span class="nl">"js"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"anseki-leader-line/leader-line.min.js"</span><span class="p">,</span><span class="s2">"content.js"</span><span class="p">],</span><span class="w">
        </span><span class="nl">"css"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"my-css.css"</span><span class="p">],</span><span class="w">
        </span><span class="nl">"matches"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"https://kurser.dtu.dk/course/*"</span><span class="p">]</span><span class="w">
        </span><span class="p">}</span><span class="w">
    </span><span class="p">],</span><span class="w">
    </span><span class="nl">"web_accessible_resources"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"resources"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"graphs.json"</span><span class="p">,</span><span class="w"> </span><span class="s2">"id_to_name.json"</span><span class="p">],</span><span class="w">
      </span><span class="nl">"matches"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"&lt;all_urls&gt;"</span><span class="p">]</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">],</span><span class="w">
    </span><span class="nl">"icons"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"16"</span><span class="p">:</span><span class="w"> </span><span class="s2">"icons/icon16.png"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"32"</span><span class="p">:</span><span class="w"> </span><span class="s2">"icons/icon32.png"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"48"</span><span class="p">:</span><span class="w"> </span><span class="s2">"icons/icon48.png"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"128"</span><span class="p">:</span><span class="w"> </span><span class="s2">"icons/icon128.png"</span><span class="w">
  </span><span class="p">}</span><span class="w">   
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>This basically defines the main settings, as well as which files to import as well (javascript and css)</p>

<p>When you have a project folder with this file within it, you can start testing the extension locally. To get it public, you first need to pay the “Chrome Web Store developer registration fee”, and then upload the project and get it approved by Google</p>

<h3 id="the-magic-of-htmlcssjs">The Magic of HTML/CSS/JS</h3>
<p>To alter the HTML content of a page, you first need to define that you need access to a certain page. The wider the permission required, the harder it might be to get the extension approved. Luckily, I only need permission to alter these pages:</p>
<blockquote>
  <p>https://kurser.dtu.dk/course/*</p>
</blockquote>

<p>We then need the <code class="language-plaintext highlighter-rouge">content.js</code> file that does the logic and rendering. The significant features are:</p>

<p><strong>1</strong>) Read the json gists that are continuously updated through a github action</p>

<p><strong>2</strong>) Run code as soon as files are loaded (using async)</p>

<p><strong>3</strong>) Extend left-most div/table (the element with course type, name, points etc.)</p>

<p><strong>4</strong>) Constructing and rendering tables, where the cells represent graph nodes (courses)</p>

<p><strong>5</strong>) Render tables in HTML</p>

<p><strong>6</strong>) Draw lines (edges) between cells, corresponding to the graph we computed. Lines are created using the “LeaderLine” package</p>

<p><strong>7</strong>) Upon hovering, highlight node and its 1-hop neighborhood (highlight immediate neighbors in both directions)</p>

<p><strong>8</strong>) Collapse graph if its too big; requiring the user to click in order to get the full view</p>

<p>In essence, we add a section to the course website, and create a table that represents the course graph. I use the awesome <strong><em>LeaderLine</em></strong> library for creating arrows (<em>directed edges</em>) between cell elements (<em>nodes</em>)</p>

<p>The result looks something like this:</p>

<p><img src="/MonthlyProjects/assets/images/courses/screenshot.png" alt="screenshot" title="screenshot" /></p>

<h2 id="maintenance"><a href="#maintenance"></a>Maintenance</h2>
<p>As of now, the extension works fine, but this is only the case for the current course structure. If anything changes with the courses or the prerequisites, the graph will technically be wrong, which may cause confusion and frustration. Let’s try and fix that.</p>

<h3 id="automatic-updates">Automatic updates</h3>
<p>I know for a fact that the course structure is changed once a year at around May or June. If I simply run the script yearly in July, then all will be good, right? Just to be certain, we run it monthly (because you never know).</p>

<p>What will likely happen is that I forget about the project, or maybe don’t really care anymore. Maybe the first years it will work fine, but after that it will not work anymore.</p>

<p>The obvious solution for this is then to create a scheduled job for scraping the course websites and create the graph. I tried some recommended websites for free python script hosting, but I also need file hosting as well. It did not go well. I then found that <strong>GitHub actions</strong> lets you do exactly that. GitHub actions is usually used for CI/CD tasks, but it doesn’t <em>have</em> to be that.</p>

<p>I have created an <code class="language-plaintext highlighter-rouge">update_courses.yml</code> workflow file. This will run all the processing the 1st of every month.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">Update Graph</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">schedule</span><span class="pi">:</span>
    <span class="c1"># Run 1st of every month</span>
    <span class="pi">-</span> <span class="na">cron</span><span class="pi">:</span> <span class="s1">'</span><span class="s">0</span><span class="nv"> </span><span class="s">0</span><span class="nv"> </span><span class="s">1</span><span class="nv"> </span><span class="s">*</span><span class="nv"> </span><span class="s">*'</span>
  <span class="na">workflow_dispatch</span><span class="pi">:</span> <span class="c1"># allows manual run</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">run-python</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>

    <span class="na">permissions</span><span class="pi">:</span>
      <span class="na">contents</span><span class="pi">:</span> <span class="s">write</span>

    <span class="na">steps</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Checkout repository</span>
      <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v3</span>
      <span class="na">with</span><span class="pi">:</span>
        <span class="na">token</span><span class="pi">:</span> <span class="s">$</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Set up Python</span>
      <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/setup-python@v4</span>
      <span class="na">with</span><span class="pi">:</span>
        <span class="na">python-version</span><span class="pi">:</span> <span class="s1">'</span><span class="s">3.12.11'</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Install dependencies</span>
      <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
        <span class="s">python -m pip install --upgrade pip</span>
        <span class="s">if [ -f requirements.txt ]; then pip install -r requirements.txt; fi</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Run Python scripts</span>
      <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
        <span class="s">cd processing</span>
        <span class="s">python 1_scrape_content.py</span>
        <span class="s">python 2_create_graph.py</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Push output file to branch</span>
      <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
        <span class="s">git config --global user.name  "github-actions[bot]"</span>
        <span class="s">git config --global user.email "github-actions[bot]@users.noreply.github.com"</span>
        <span class="s">git add jsons/valid_courses.json</span>
        <span class="s">git add jsons/id_to_name.json</span>
        <span class="s">git add jsons/graphs.json</span>
        <span class="s">git commit -m "Update output for $(date +'%Y-%m-%d')"</span>
        <span class="s">git push -u origin master --force</span>

</code></pre></div></div>

<p>The coolest thing is that you let a “bot user” push files to the repo. So instead of adding the graph JSON file directly into the Chrome extension, the file will be located and updated at a specific file in the repo, which is directly accessible from the web (since its a public repo). It will basically act as a GitHub “gist” file.</p>

<h3 id="open-sourcing">Open-Sourcing</h3>
<p>To be realistic, I can’t assume that people will agree with the design of the extension, so I want to give people the option to add improvements to it. It is a gift for the public after all. In the event that the course website undergoes a drastic change that renders the extension useless, I want people in the future to be able to fix that as well. I will, of course, be responsible for reviewing pull requests.</p>

<p>Maybe no one cares. Who knows? Now that I have this tool, I can’t imagine a world without it anymore. I want everyone to get the same benefits as I got from it. It was a fun project nonetheless.</p>]]></content><author><name>Marcus Presutti</name><email>marcus.presutti.eu@gmail.com</email></author><summary type="html"><![CDATA[Create a chrome extension for the DTU course website]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4000/MonthlyProjects/courses/screenshot.png" /><media:content medium="image" url="http://localhost:4000/MonthlyProjects/courses/screenshot.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">4. RAG model for Danish court trial data (Part 2/2)</title><link href="http://localhost:4000/MonthlyProjects/court_trial_ai_B" rel="alternate" type="text/html" title="4. RAG model for Danish court trial data (Part 2/2)" /><published>2025-07-01T00:00:00+02:00</published><updated>2025-07-01T00:00:00+02:00</updated><id>http://localhost:4000/MonthlyProjects/court_trial_ai_B</id><content type="html" xml:base="http://localhost:4000/MonthlyProjects/court_trial_ai_B"><![CDATA[<h2 id="cloud"><a href="#cloud"></a>Cloud</h2>
<p>At first, the reason for using the cloud was primarily just to learn more about it, despite thinking that it won’t be necessary, but it was actually quite beneficial. First of all, it enabled me to host an endpoint for the model, which makes it possible to interact with it from anywhere in the world. But most importantly, it was actually quite nice to offload the data processing to a cloud computer. Not only would my local PC have been unavailable for proper use, the computation also happened much faster, since the cheaper VMs ran the processing way faster than my own PC, and also did not encounter any crashes. It took Azure 3½ hours using some of the cheapest compute, but mine had not even finished after 5+ hours locally.</p>

<p><img src="/MonthlyProjects/assets/images/domstol/job_script.png" alt="job_script" title="job_script" /></p>

<p>I had initially started working on Machine Learning Operations (MLOps) at a DTU course of the same name, where we tried doing some simple ML workflows on Google Cloud Platform, but did not utilize the ML-specific services. Later on, I got some experience in Azure, working in the ML team at GN. At GN, I went through Microsoft’s official tutorials, and finally got to test it out during this project.</p>

<p>Azure has everything we can ask for.</p>
<ol>
  <li>It provides a <strong>storage account</strong> for <em>blob storage</em></li>
  <li><strong>environments</strong> that VMs such as endpoints and job scripts can utilize</li>
  <li>In Azure ML Studio we get many features such as <strong>notebooks</strong>, <strong>job scripts</strong>, <strong>endpoint hosting</strong> etc. It tries to encapsulate the entire ML workflow from start to finish.</li>
</ol>

<p>This is my attempt at creating the equivalent workflow, from before, in the cloud:</p>

<p><img src="/MonthlyProjects/assets/images/domstol/workflow_cloud.png" alt="workflow_cloud" title="workflow_cloud" /></p>

<h3 id="cost-management">Cost management</h3>
<p>In Azure, you create an endpoint, but you also need to actually <em>deploy</em> a VM that acts as our API server. It should be noted that they run until you turn them off. I had falsely assumed that it was stationary until someone did an API call (cold start), which in turn cost me around 900 DKK.</p>

<p>For the overall expendeture of the project, I present the full overview:</p>

<p><img src="/MonthlyProjects/assets/images/domstol/azure_costs.png" alt="azure_costs" title="azure_costs" /></p>

<ul>
  <li>~900 kr. was spent on <strong>Compute</strong>, which is every time a computer is running computation. This is notebooks, job scripts, building environments and endpoint deployments etc.</li>
  <li>~100 kr. was spent on <strong>Storage</strong>, which is for storing the files I have in BLOB storage. This is the text files, FAISS index, and meta data files.</li>
  <li>~30 kr. was spent on <strong>Networking</strong>, which is the cost of bandwidth of data leaving Azure. This must accumulate from multiple sources, as there is not an obvious resource that consumes a ton of bandwidth.</li>
  <li>~30 kr. was spent on <strong>Containers</strong>, which is basically compute used in (emphemeral) containers. This would be the serverless job scripts that I used for processing.</li>
</ul>

<h2 id="results"><a href="#results"></a>Results</h2>
<p>Now that the endpoint is up an running, we can try an inspect the results of the following prompt:</p>

<blockquote>
  <p><strong>(DK) Prompt: “Tiltalt for at besidde våben og stoffer”</strong> <br />
<strong>(EN) Prompt: “Charged with possession of weapons and drugs”</strong></p>
</blockquote>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">requests</span>
<span class="kn">import</span> <span class="nn">json</span>

<span class="n">endpoint</span> <span class="o">=</span> <span class="s">"&lt;ENDPOINT-URL&gt;"</span> 
<span class="n">api_key</span>  <span class="o">=</span> <span class="s">"&lt;API-KEY&gt;"</span>  

<span class="n">headers</span> <span class="o">=</span> <span class="p">{</span>
    <span class="s">"Content-Type"</span><span class="p">:</span> <span class="s">"application/json"</span><span class="p">,</span>
    <span class="s">"Authorization"</span><span class="p">:</span> <span class="sa">f</span><span class="s">"Bearer </span><span class="si">{</span><span class="n">api_key</span><span class="si">}</span><span class="s">"</span>
<span class="p">}</span>

<span class="n">data</span> <span class="o">=</span> <span class="p">{</span>
    <span class="s">"prompt"</span><span class="p">:</span> <span class="s">"Tiltalt for at besidde våben og stoffer"</span>
<span class="p">}</span>

<span class="n">response</span> <span class="o">=</span> <span class="n">requests</span><span class="p">.</span><span class="n">post</span><span class="p">(</span><span class="n">endpoint</span><span class="p">,</span> <span class="n">headers</span><span class="o">=</span><span class="n">headers</span><span class="p">,</span> <span class="n">json</span><span class="o">=</span><span class="n">data</span><span class="p">)</span>
<span class="k">print</span><span class="p">(</span><span class="n">response</span><span class="p">.</span><span class="n">json</span><span class="p">())</span>
</code></pre></div></div>

<p>This request retrieves the 5 best matches, from best to worst, in a JSON style format. To make it more readable here, I’ve omitted some numbers, and formatted it neatly:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>RESULT 1)
'HEADLINE'     : 'Tiltale for bl.a. forsøg på manddrab ved at have planlagt at dræbe flere personer på skoler m.v. ved skyderier. Påstand om konfiskation'
'CASE_SUBJECTS': 'Strafferetlige sanktioner og andre foranstaltninger. Våben, eksplosiver og fyrværkeri. Liv og legeme'
'CHUNK_TEXT'   : 'at have opnået våbentilladelse og tilladelse til at opbevare våben'
'DISTANCE'     : '0.7364452'
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>RESULT 2)
'HEADLINE'     : 'Landsretten stadfæstede byrettens dom i sag om tiltale for overtrædelse af lov om euforiserende stoffer § 3, stk. 1, jf. § 2, stk. 4, jf. bekendtgørelse om euforiserende stoffer § 30, stk. 1, jf. § 3, stk. 2, jf. bilag 1, liste B, nr. 59 og straffelovens §279 a.'
'CASE_SUBJECTS': 'Narkotika. Formueforbrydelser'
'CHUNK_TEXT'   : 'vens § 191, stk. 1, 1. pkt., våbenbekendtgørelsen, lov om visse dopingmidler og lovgivningen om euforiserende stoffer.'
'DISTANCE'     : '0.7441238'
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>RESULT 3)
'HEADLINE'     : 'Landsrettens dom i sag om overtrædelse af straffelovens § 192a, stk. 1, nr. 1, jf. til dels stk. 3, jf. våbenlovens § 10, stk. 1, jf. § 2, stk. 1, jf. § 1, stk. 1, nr. 1 - 3 mv. stadfæstes med den ændring, at tiltalte straffes med fængsel i 3 år'
'CASE_SUBJECTS': 'Våben, eksplosiver og fyrværkeri. Strafferetlige sanktioner og andre foranstaltninger. Udlændinge. Narkotika'
'CHUNK_TEXT'   : 'overdragelse og i overtrædelse af våbenlovgivningen ved at have været i besiddelse af en kre-ditkortkniv, et knojern og to peberspray.'
'DISTANCE'     : '0.7442008'
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>RESULT 4)
'HEADLINE'     : 'Tiltale for overtrædelse af bl.a. straffelovens § 191, stk. 1, 2. pkt., jf. til dels stk. 2, jf. lov om euforiserende stoffer § 3, stk.1, jf. § 2, stk. 4, jf. bekendtgørelse om euforiserende stoffer § 30 (dagældende § 27), jf. § 3, jf. bilag 1, liste B, nr. 70 samt straffelovens § 192a, stk. 1, nr. 1, jfr. stk. 3, jfr. våbenlovens § 10, stk. 1, jfr. § 1, stk. 1, nr. 1, 2 og 3. Påstand om konfiskation'
'CASE_SUBJECTS': 'Narkotika. Våben, eksplosiver og fyrværkeri. Formueforbrydelser. Forbrydelser mod offentlig myndighed'
'CHUNK_TEXT'   : 'Lange fængselsstraffe for salg af kokain og besiddelse af skydevåben'
'DISTANCE'     : '0.79919827'
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>RESULT 5)
'HEADLINE'     : 'Tiltale for narko- og våbenbesiddelse. Påstand om konfiskation'
'CASE_SUBJECTS': 'Narkotika. Våben, eksplosiver og fyrværkeri. Færdsel. Strafferetlige sanktioner og andre foranstaltninger'
'CHUNK_TEXT'   : 'Om våben og ammunition (forhold 1)'
'DISTANCE'     : '0.8109229'
</code></pre></div></div>

<p>In the output, the “CHUNK_TEXT” is what is actually being compared with the prompt, and “DISTANCE” defined how close the sentences are in vector space.</p>

<p>Even if the chunk-text in result 2 and 4 are the only ones that actually mentions both weapons and drugs, the “case subjects” usually include both. This is purely coincidental.
It’s hard to say why only 2 and 4 seem valid, as the distance measure doesn’t come with a good explanation. Result 1 does not even have “drugs” in the case subject. The embeddings might have had a focus on something different. To maybe better understand these embeddings, let’s try visualizing them.</p>

<h3 id="pca-projection">PCA projection</h3>

<p>To get an intuitive feel of how close the vector are, other than the distance itself, we can display all the embeddings as a 2D prjection using PCA. This means that all the information of the 384 dimensions have been cramped into 2 dimensions, or <em>principal components</em> (PC). These PCs are basically combinations of all 384 dimensions, but are supposed to “span the 2D space of most information”.</p>

<p>It would seem that the chunks/points that are closest in the full space are also quite close in the 2 principal directions/dimensions:</p>

<p><img src="/MonthlyProjects/assets/images/domstol/pca_projection.jpeg" alt="pca_projection" title="pca_projection" /></p>

<p>But notice that the results 1 and 4 are the most dissimilar to the prompt, in PCA space, while 2,3 and 5 are more similar (also to each other). We should understand this as: By only considering the 2 most important “directions of information”, results 2,3 and 5 have more in common with the prompt. The compression into 2 dimensions is only meant to be guiding, and used for visualization, but it can actually be useful to reduce it to something like 100 dimensions, as the remaining dimensions might be considered noise.</p>

<p><img src="/MonthlyProjects/assets/images/domstol/explained_variance.jpeg" alt="explained_variance" title="explained_variance" /></p>

<p>The graph above shows how much information is kept, by reducing the dataset to K components. Using 100 out of 384 PCs, we keep around 80% information. Experience tells me that this dataset can’t really be compressed, as all the dimensions have a quite big impact, since the curve is not very start for low K’s. This would suggest that the embeddings don’t have a few latent variables that explain most of the information.</p>

<h2 id="reflection"><a href="#reflection"></a>Reflection</h2>
<p>The RAG is quite limited by the Danish embeddings, even though it did alright. I should definitely also have included the headline as a valid chunk that could be compared to the prompt embedding, but I did not think of that in time. The RAG could be improved further if I pushed the limit on how much text I would assign to each chunk. The current paragraphs/chunks don’t even come near the ~400 word limit. I also imagine that doing the project on a translated version of the court data might actually have been better, instead of training an embedding model on Danish data. The language is definitely a bottleneck in making this work perfectly, but not necessarily the first bottleneck.</p>

<h3 id="assesment-of-cloud-computing">Assesment of cloud computing</h3>
<p>The idea of having everything hosted on the cloud in a pay-as-you-go fashion is quite appealing. In theory it’s great, but in practice it has been a mess to get working. It’s definitely more clear to me what to do in the future, but it still requires a solid plan as you quickly lose the overview.</p>

<p>First of all, you have to be careful what resources are running, which is not displayed in a clear way. Also, the Azure SDK (libraries, whatever) are very confusing, as they have 2 very different versions that tend to conflict with each other. Everything worked when I was consistent at using SDKv2 only, but it was very unclear at first that there was this distinction.</p>

<p>Another thing to consider is the feedback of doing changes. Azure ML does provide cloud notebooks which lets one run code directly in the cloud, but they take some time to get up and running. For almost any other thing, it would take a long time to see if it worked. This is because things behave differently on the cloud, as they need certain access rights, and code has to work with the online environment etc. . When working locally, things usually just work. The biggest reason for using the cloud is that its available almost 100% of the time. The cloud computers don’t need to be turned off, which makes server hosting and automatic script execution very easy.</p>

<h3 id="final-thoughts">Final thoughts</h3>
<p>This has been a very large project, and I have learned a ton. I never expected the results to be any good, as the methods used/learned were important in themselves. I have always struggled with cloud computing, but I think this experience helped me figure it out. I am still interested in working with court data, but that would be in the context of creating an AI to assist or replace the classic role of the judge.</p>]]></content><author><name>Marcus Presutti</name><email>marcus.presutti.eu@gmail.com</email></author><summary type="html"><![CDATA[(Part 2/2) Hosting the model in the cloud]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4000/MonthlyProjects/domstol/standoff.png" /><media:content medium="image" url="http://localhost:4000/MonthlyProjects/domstol/standoff.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">3. RAG model for Danish court trial data (Part 1/2)</title><link href="http://localhost:4000/MonthlyProjects/court_trial_ai_A" rel="alternate" type="text/html" title="3. RAG model for Danish court trial data (Part 1/2)" /><published>2025-06-01T00:00:00+02:00</published><updated>2025-06-01T00:00:00+02:00</updated><id>http://localhost:4000/MonthlyProjects/court_trial_ai_A</id><content type="html" xml:base="http://localhost:4000/MonthlyProjects/court_trial_ai_A"><![CDATA[<h2 id="prologue"><a href="#prologue"></a>Prologue</h2>
<p>Ethics regarding the use of AI for decision making in court, has been one of my favorite topics to discuss, as the philosophy of intelligence, conciousness, and emotions in AI has had a big impact on my world view.</p>

<p>As an engineer, I would also like to see things in practice, rather than just theorize about things. This is what drove me to start a project about AI with public Danish court trial data. Ideally, the available data would be structuered in a way similar to datasets in supervised learning, where we have a text description of the case, and a label corresponding to the punishment. Unfortunately, this was not the case, as the data consists of text documents that are not defined in a specific template. Therefore, the goal became to make a RAG model for retrieving these court documents based on a search prompt. I.e. to do natural text search on these documents. I want to do this using local models, and without LLM-pipeline libraries like LangChain.</p>

<p>As the ambition level of the project fell, I had to challenge myself in another way to keep it exciting. I chose to utilize cloud computing for every step of the project, including testing, data processing, storage, and even endpoint hosting, primarily using Azure Machine Learning. I have always struggled with using cloud computing succesfully, so this is a perfect chance to give it another shot.</p>

<h2 id="data-gathering"><a href="#data-gathering"></a>Data Gathering</h2>
<p>In Denmark, we recently acquired a public database of court case documents, which are accesible through the <a href="https://domsdatabasen.dk/">website</a> or the <a href="https://domsdatabasen.dk/spoergsmaal-og-svar/api-adgang-til-domsdatabasen/">API</a>. Although not exhaustive, it contains many cases from recent years. We will use the API to collect a local copy of all the data.</p>

<p>We first need to authenticate:</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">requests</span>
<span class="kn">from</span> <span class="nn">dotenv</span> <span class="kn">import</span> <span class="n">dotenv_values</span>
<span class="n">config</span> <span class="o">=</span> <span class="n">dotenv_values</span><span class="p">(</span><span class="s">".env"</span><span class="p">)</span>

<span class="c1"># Authenticate with the API
</span><span class="n">url</span> <span class="o">=</span> <span class="s">"https://domsdatabasen.dk/webapi/kapi/v1/autoriser"</span>
<span class="n">headers</span> <span class="o">=</span> <span class="p">{</span><span class="s">"Content-Type"</span><span class="p">:</span> <span class="s">"application/json"</span><span class="p">}</span>
<span class="n">body</span> <span class="o">=</span> <span class="p">{</span><span class="s">"Email"</span><span class="p">:</span> <span class="n">config</span><span class="p">[</span><span class="s">"username"</span><span class="p">],</span> <span class="s">"Password"</span><span class="p">:</span> <span class="n">config</span><span class="p">[</span><span class="s">"password"</span><span class="p">]}</span>
<span class="n">response</span> <span class="o">=</span> <span class="n">requests</span><span class="p">.</span><span class="n">post</span><span class="p">(</span><span class="n">url</span><span class="p">,</span> <span class="n">json</span><span class="o">=</span><span class="n">body</span><span class="p">,</span> <span class="n">headers</span><span class="o">=</span><span class="n">headers</span><span class="p">)</span>

<span class="c1"># Check if the response is successful
</span><span class="k">if</span> <span class="n">response</span><span class="p">.</span><span class="n">status_code</span> <span class="o">==</span> <span class="mi">200</span><span class="p">:</span>
    <span class="n">data</span> <span class="o">=</span> <span class="n">response</span><span class="p">.</span><span class="n">json</span><span class="p">()</span>
    <span class="k">print</span><span class="p">(</span><span class="s">"Authorization successful!"</span><span class="p">)</span>
    <span class="k">print</span><span class="p">(</span><span class="s">"User ID:        "</span><span class="p">,</span> <span class="n">data</span><span class="p">[</span><span class="s">"userId"</span><span class="p">])</span>
    <span class="k">print</span><span class="p">(</span><span class="s">"User Name:      "</span><span class="p">,</span> <span class="n">data</span><span class="p">[</span><span class="s">"username"</span><span class="p">])</span>
    <span class="k">print</span><span class="p">(</span><span class="s">"Issued UTC time:"</span><span class="p">,</span> <span class="n">data</span><span class="p">[</span><span class="s">"issuedUtcTime"</span><span class="p">])</span>
    <span class="n">token</span> <span class="o">=</span> <span class="n">data</span><span class="p">[</span><span class="s">"tokenString"</span><span class="p">]</span>
    <span class="k">print</span><span class="p">(</span><span class="s">"Token:          "</span><span class="p">,</span> <span class="n">token</span><span class="p">)</span>
<span class="k">else</span><span class="p">:</span>
    <span class="k">print</span><span class="p">(</span><span class="s">"Error:"</span><span class="p">,</span> <span class="n">response</span><span class="p">.</span><span class="n">status_code</span><span class="p">,</span> <span class="n">response</span><span class="p">.</span><span class="n">text</span><span class="p">)</span>
</code></pre></div></div>

<p>And then we can fetch the data with a series of GET requests:</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">headers</span> <span class="o">=</span> <span class="p">{</span><span class="s">'Authorization'</span><span class="p">:</span> <span class="sa">f</span><span class="s">'Bearer </span><span class="si">{</span><span class="n">token</span><span class="si">}</span><span class="s">'</span><span class="p">}</span>
<span class="n">sideNr</span> <span class="o">=</span> <span class="mi">1</span>

<span class="k">while</span> <span class="bp">True</span><span class="p">:</span>
  
    <span class="n">params</span> <span class="o">=</span> <span class="p">{</span>
        <span class="s">'sideNr'</span><span class="p">:</span> <span class="n">sideNr</span><span class="p">,</span>
        <span class="s">'perSide'</span><span class="p">:</span> <span class="mi">25</span>
    <span class="p">}</span>
    <span class="n">response</span> <span class="o">=</span> <span class="n">requests</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="n">url</span><span class="p">,</span> <span class="n">headers</span><span class="o">=</span><span class="n">headers</span><span class="p">,</span> <span class="n">params</span><span class="o">=</span><span class="n">params</span><span class="p">)</span>

    <span class="k">if</span> <span class="n">response</span><span class="p">.</span><span class="n">status_code</span> <span class="o">==</span> <span class="mi">200</span><span class="p">:</span>
        <span class="n">data</span> <span class="o">=</span> <span class="n">response</span><span class="p">.</span><span class="n">json</span><span class="p">()</span>
    <span class="p">...</span>
    <span class="n">sideNr</span> <span class="o">+=</span> <span class="mi">1</span>
</code></pre></div></div>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># For each page, extract case data
</span><span class="k">for</span> <span class="n">item</span> <span class="ow">in</span> <span class="n">data</span><span class="p">:</span>

    <span class="c1"># For each case, extract documents
</span>    <span class="k">for</span> <span class="n">doc</span> <span class="ow">in</span> <span class="n">item</span><span class="p">[</span><span class="s">'documents'</span><span class="p">]:</span>
        <span class="p">...</span>
</code></pre></div></div>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Extract the HTML content of the document
</span><span class="n">document</span> <span class="o">=</span> <span class="n">doc</span><span class="p">[</span><span class="s">'contentHtml'</span><span class="p">]</span>
<span class="k">if</span> <span class="ow">not</span> <span class="n">document</span><span class="p">:</span> <span class="k">continue</span>
<span class="n">all_documents</span><span class="p">.</span><span class="n">append</span><span class="p">(</span><span class="n">document</span><span class="p">)</span>

<span class="c1"># Use BS4 to extract paragraphs
</span><span class="n">bs</span> <span class="o">=</span> <span class="n">BeautifulSoup</span><span class="p">(</span><span class="n">document</span><span class="p">,</span> <span class="s">'html.parser'</span><span class="p">)</span>
<span class="n">text</span> <span class="o">=</span> <span class="s">''</span>
<span class="k">for</span> <span class="n">page</span> <span class="ow">in</span> <span class="n">bs</span><span class="p">.</span><span class="n">find_all</span><span class="p">(</span><span class="s">'div'</span><span class="p">,</span> <span class="n">class_</span><span class="o">=</span><span class="s">'page'</span><span class="p">):</span>
    <span class="n">text</span> <span class="o">+=</span> <span class="n">page</span><span class="p">.</span><span class="n">get_text</span><span class="p">(</span><span class="n">separator</span><span class="o">=</span><span class="s">"</span><span class="se">\n</span><span class="s">"</span><span class="p">).</span><span class="n">strip</span><span class="p">()</span> <span class="o">+</span> <span class="s">'</span><span class="se">\n</span><span class="s">'</span>

<span class="c1"># Write each document to a .txt file (both text and HTML)
</span><span class="n">text_to_txt</span><span class="p">(</span><span class="n">text</span><span class="p">,</span>     <span class="sa">f</span><span class="s">'</span><span class="si">{</span><span class="n">texts_dir</span><span class="si">}</span><span class="s">/</span><span class="si">{</span><span class="n">doc</span><span class="p">[</span><span class="s">"id"</span><span class="p">]</span><span class="si">}</span><span class="s">'</span><span class="p">)</span>
<span class="n">text_to_txt</span><span class="p">(</span><span class="n">document</span><span class="p">,</span> <span class="sa">f</span><span class="s">'</span><span class="si">{</span><span class="n">texts_dir</span><span class="o">+</span><span class="s">'_html'</span><span class="si">}</span><span class="s">/</span><span class="si">{</span><span class="n">doc</span><span class="p">[</span><span class="s">"id"</span><span class="p">]</span><span class="si">}</span><span class="s">'</span><span class="p">)</span>
</code></pre></div></div>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Save meta data
</span><span class="n">all_meta</span><span class="p">.</span><span class="n">append</span><span class="p">([</span><span class="n">item</span><span class="p">[</span><span class="s">'headline'</span><span class="p">],</span>
                <span class="s">'?'</span><span class="p">.</span><span class="n">join</span><span class="p">([</span><span class="n">subject</span><span class="p">[</span><span class="s">'displayText'</span><span class="p">]</span> <span class="k">for</span> <span class="n">subject</span> <span class="ow">in</span> <span class="n">item</span><span class="p">[</span><span class="s">'caseSubjects'</span><span class="p">]]),</span>
                <span class="nb">int</span><span class="p">(</span><span class="n">item</span><span class="p">[</span><span class="s">'id'</span><span class="p">]),</span>
                <span class="nb">int</span><span class="p">(</span><span class="n">doc</span><span class="p">[</span><span class="s">'id'</span><span class="p">]),</span>
                <span class="n">doc</span><span class="p">[</span><span class="s">'displayTitle'</span><span class="p">],</span>
                <span class="n">doc</span><span class="p">[</span><span class="s">'verdictDateTime'</span><span class="p">],</span>
                <span class="n">item</span><span class="p">[</span><span class="s">'closedCourtroom'</span><span class="p">],</span>
                <span class="n">item</span><span class="p">[</span><span class="s">'profession'</span><span class="p">][</span><span class="s">'displayText'</span><span class="p">],</span>
                <span class="n">item</span><span class="p">[</span><span class="s">'instance'</span><span class="p">][</span><span class="s">'displayText'</span><span class="p">],</span>
                <span class="n">item</span><span class="p">[</span><span class="s">'caseType'</span><span class="p">][</span><span class="s">'displayText'</span><span class="p">]</span>
                <span class="p">])</span>
</code></pre></div></div>
<p>The filters available in the request header is quite limited. We can extract 25 documents per page, which is reasonable, but we are not able to filter by time, or any other relevant attributes. Also, sorting is not possible. This is also why we need to extract every document, since we can’t guarantee which document we get otherwise.</p>

<p>I have added a small overview of all the important attributes. Some of them are attributes for the <em>case</em>, while some are attributes of the actual <em>documents</em> associated with a case:</p>

<table>
  <thead>
    <tr>
      <th><strong>Attribute</strong></th>
      <th><strong>Type</strong></th>
      <th><strong>Description</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>headline</td>
      <td><em>case</em></td>
      <td>Headline of the case</td>
    </tr>
    <tr>
      <td>caseSubjects</td>
      <td><em>case</em></td>
      <td>Themes/topics of a case (violence, drugs, taxes etc.)</td>
    </tr>
    <tr>
      <td>id</td>
      <td><em>case</em></td>
      <td>Unique ID for a case</td>
    </tr>
    <tr>
      <td>id</td>
      <td><em>document</em></td>
      <td>Unique ID for a document</td>
    </tr>
    <tr>
      <td>displayTitle</td>
      <td><em>document</em></td>
      <td>Title of document</td>
    </tr>
    <tr>
      <td>verdictDateTime</td>
      <td><em>document</em></td>
      <td>Time of verdict (as defined in the document)</td>
    </tr>
    <tr>
      <td>closedCourtroom</td>
      <td><em>case</em></td>
      <td>Whether the courtroom has been closed from public or not</td>
    </tr>
    <tr>
      <td>profession</td>
      <td><em>case</em></td>
      <td>Type of case (criminal case, foreclosure etc.)</td>
    </tr>
    <tr>
      <td>instance</td>
      <td><em>case</em></td>
      <td>Stage of court (District court, High court, or Supreme) court</td>
    </tr>
    <tr>
      <td>caseType</td>
      <td><em>case</em></td>
      <td>More specifically what category the case is</td>
    </tr>
  </tbody>
</table>

<h2 id="data-processing"><a href="#data-processing"></a>Data Processing</h2>
<p>So, we split the response data into the meta data and the document data. The meta data can easily be described in a table, and will therefore be saved as a parquet-file (instead of csv) in order to save disk space. To further optimize space/memory usage, the column data types are selected manually.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Convert data types for efficiency
</span><span class="n">df_meta</span><span class="p">[</span><span class="s">'id'</span><span class="p">]</span>           <span class="o">=</span> <span class="n">df_meta</span><span class="p">[</span><span class="s">'id'</span><span class="p">].</span><span class="n">astype</span><span class="p">(</span><span class="s">'uint32'</span><span class="p">)</span>
<span class="n">df_meta</span><span class="p">[</span><span class="s">'doc_id'</span><span class="p">]</span>       <span class="o">=</span> <span class="n">df_meta</span><span class="p">[</span><span class="s">'doc_id'</span><span class="p">].</span><span class="n">astype</span><span class="p">(</span><span class="s">'uint32'</span><span class="p">)</span>
<span class="n">df_meta</span><span class="p">[</span><span class="s">'verdict_date'</span><span class="p">]</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="n">to_datetime</span><span class="p">(</span><span class="n">df_meta</span><span class="p">[</span><span class="s">'verdict_date'</span><span class="p">])</span>
<span class="n">df_meta</span><span class="p">[</span><span class="s">'doc_type'</span><span class="p">]</span>     <span class="o">=</span> <span class="n">df_meta</span><span class="p">[</span><span class="s">'doc_type'</span><span class="p">].</span><span class="n">astype</span><span class="p">(</span><span class="s">'category'</span><span class="p">)</span>
<span class="n">df_meta</span><span class="p">[</span><span class="s">'profession'</span><span class="p">]</span>   <span class="o">=</span> <span class="n">df_meta</span><span class="p">[</span><span class="s">'profession'</span><span class="p">].</span><span class="n">astype</span><span class="p">(</span><span class="s">'category'</span><span class="p">)</span>
<span class="n">df_meta</span><span class="p">[</span><span class="s">'instance'</span><span class="p">]</span>     <span class="o">=</span> <span class="n">df_meta</span><span class="p">[</span><span class="s">'instance'</span><span class="p">].</span><span class="n">astype</span><span class="p">(</span><span class="s">'category'</span><span class="p">)</span>
<span class="n">df_meta</span><span class="p">[</span><span class="s">'case_type'</span><span class="p">]</span>    <span class="o">=</span> <span class="n">df_meta</span><span class="p">[</span><span class="s">'case_type'</span><span class="p">].</span><span class="n">astype</span><span class="p">(</span><span class="s">'category'</span><span class="p">)</span>

<span class="c1"># Save to .parquet
</span><span class="n">df_meta</span><span class="p">.</span><span class="n">to_parquet</span><span class="p">(</span><span class="s">'meta.parquet'</span><span class="p">,</span> <span class="n">index</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
</code></pre></div></div>

<p>This is a somewhat neglible addition, as the table is ~5000x10. But it’s still a good principle, and I also just learned these tricks from my “Python and High-peformance Computing” course, so I wanted to see it in practice.</p>

<p>As for the actual documents, they are stored in HTML format. The text itself can easily be extracted using the Beautiful Soup (bs4) library. It is also beneficial to split the text into small paragraphs, such that they can later be used for “chunking” (more on that later). It is easy to make these splits, as each actual paragraph is given by the HTML paragraph- or ‘p’ tag.</p>

<p>Now, the most computationally demanding processing step is to convert each of these paragraphs into an embedding vector, which can take multiple hours. We explain the embedding vectors next.</p>

<h3 id="sentence-embeddings">Sentence Embeddings</h3>
<p>Building a RAG model requires that we can convert our prompt, as well as the reference material, into a vector representation, such that they can be compared. This representation is formally called an embedding. We need ML embedding models to perform this transformation.</p>

<p>As we are working with a Danish dataset, it is not possible to simply use the default models, as they are usually in English. Even so-called “multi-lingual” models can still struggle with Danish, as the Danish language is quite underrepresented on the internet. My best attempt was to use a model from Huggingface that was explicitly fine-tuned for the Danish language (<a href="https://huggingface.co/KennethTM/MiniLM-L6-danish-encoder">link</a>). It’s definitely not perfect, but it’s alright.</p>

<p>A limitation I have encountered is that the tokenization of words seems a bit wrong. In terms of vector similarity, the word “Kølle” (“club”, as in the weapon) and “Køleskab” (“refrigerator”) are way too similar due to the <em>køl</em> -token. Let’s consider this table of distances between embeddings (meaning that lower values indicate more similarity)</p>

<p><img src="/MonthlyProjects/assets/images/domstol/embedding_matrix.png" alt="embedding_matrix" title="embedding_matrix" /></p>

<p>Vector similarity is somewhat complicated, since embeddings might actually be similar in one way, but not the way <em>I</em> want. In the matrix above we see that “bat” (bat), “stav” (rod/stick), and “hammer” (hammer) are somewhat similar to each other, but not similar to “Kølle”.</p>

<h3 id="theory-training-an-embedding-model">(Theory) Training an embedding model</h3>
<p>Let’s get into a bit more details regarding this embedding model. For basically any NLP (Natural Language Processing) -related task, we should use a transformer-based model. The most popular one would be the GPT-models that are used for the famous chatbots. But GPT-models are inherently text prediction models which are good for <em>generating</em> text, but not necessarily <em>classifying</em> text. On the other hand, BERT-models are good at classification as it takes an entire sequence and analyses each part of the sequence from both left-to-right, but also right-to-left, making them <em>bi-directional</em> (the B in BERT). Our embedding model is therefore a fine-tuned BERT model for (Danish) sentence embeddings, aka. Sentence-BERT.</p>

<p>To optimzie the weights of a deep neural network, we need a <em>loss-function</em> that defines how well the task is being solved during training, in order to nudge the weights in the right direction. This model has been trained using a <em>contrastive</em> loss function, which we need to minimize (but the fraction should be maximzied, so to speak):</p>

<p><img src="/MonthlyProjects/assets/images/domstol/contrastive_loss.png" alt="contrastive_loss" title="contrastive_loss" /></p>

<p>Let’s understand this. The training dataset consist of N pairs of sentences, where each element (A and B) in a pair is somehow related to each other. Given the current state of the model, we try to embed A, B, and many other sentences. The goal is the make the embedding of A and B more similar (numerator), while letting A and the other sentences become more dissimilar (denominator). Note that the vector operation between embeddings is the <em>inner product</em>, which is also called the <em>cosine similarity</em>:</p>

<p><img src="/MonthlyProjects/assets/images/domstol/inner_product.png" alt="inner_product" title="inner_product" /></p>

<p>This loss is then propagated backwards through the network, and the weights are updated accordingly. Whether sentences are similar or not in the final model is hugely dependent on what the dataset has defined as being similar.</p>

<p>The model we are using is quite light-weight, which was probably to make fine-tuning to the Danish dataset easier. This means that our model embedding-dimension is (only) of size 384, but the maximum sequence (input) length is 512 tokens, which is actually a good amount (~400 words). This should make sure that all of our paragraphs fit individually, but not entire court trial documents.</p>

<p>Recall that our task is to find the trial document that best matches our prompt. We are quite limited by this token size, as we can’t simply embed an entire document into a single vector (that would likely also require a larger embedding size). Instead, we will try and embed each paragraph of the documents, and track which paragraph belongs to which document. This is called “chunking”, for which there are multiple strategies for how to embed these subtexts. We are lucky to already have beautifully split paragraphs in the trial documents, but for long texts, another strategy could be to embed overlapping subtexts of a fixed window-size, and a fixed overlap-size.</p>

<h2 id="rag-model"><a href="#rag-model"></a>RAG Model</h2>

<p>Let’s make it completely clear what we want to achieve using this model:</p>

<blockquote>
  <p><strong>A list of the k most relevant documents wrt. a prompt, given the similarity between the prompt and a single paragraph in these documents.</strong></p>
</blockquote>

<p>It is, however, quite common to extend a chat-bot with RAG capabilities, but not necessary. To do this, the RAG model should return some text, which can then be injected into the “system prompt” (not the “user prompt”). A simplified example could be:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>USER PROMPT       : "What is the worst punishment a burglar has received?"
&lt;RAG&gt;             : *search for relevant documents*
&lt;RAG&gt;             : *returns [doc_A, doc_B, doc_C]*
SYSTEM            : "Your job is to answer questions about the following court trials: {content of doc_A, doc_B, doc_C}
ASSITANT RESPONSE : "Here is the worst punishment a burglar has received: &lt;...&gt;"
</code></pre></div></div>

<p>If we were to do this ourselves, we should be cautious of:</p>
<ol>
  <li>The quality of the LLM used, due to challenges with the Danish language</li>
  <li>Which information the RAG returns, as the entire reference document is too large to fit within the system prompt</li>
</ol>

<p>Anyway, we are keeping it simple.</p>

<p>We have not talked about the challenge of searching for similar documents, which is very interesting. The straight forward way to find the most similar vectors wrt. the prompt vector, would be to simply check all options by brute force. In itself, this is slow, but using the FAISS (Facebook AI Similarity Search) library, many optimizations have been applied to speedup even the brute-force approach. It’s very memory efficient, and it utilizes multiprocessing in order to parallelize the computation. They also offer approximate solutions for large datasets, where brute force in not feasible.</p>

<p>As we have covered all steps of the process, let’s show a over-simplified flowchart:</p>

<p><img src="/MonthlyProjects/assets/images/domstol/workflow_local.png" alt="workflow_local" title="workflow_local" /></p>]]></content><author><name>Marcus Presutti</name><email>marcus.presutti.eu@gmail.com</email></author><summary type="html"><![CDATA[(Part 1/2) A model for retrieving relevant historic trials, given short text description]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4000/MonthlyProjects/domstol/workflow_local.png" /><media:content medium="image" url="http://localhost:4000/MonthlyProjects/domstol/workflow_local.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">2. Shortcut Removal using Diffusion Models</title><link href="http://localhost:4000/MonthlyProjects/shortcut_removal_diffusion" rel="alternate" type="text/html" title="2. Shortcut Removal using Diffusion Models" /><published>2025-05-01T00:00:00+02:00</published><updated>2025-05-01T00:00:00+02:00</updated><id>http://localhost:4000/MonthlyProjects/shortcut_removal_diffusion</id><content type="html" xml:base="http://localhost:4000/MonthlyProjects/shortcut_removal_diffusion"><![CDATA[<h2 id="prologue"><a href="#prologue"></a>Prologue</h2>
<p>May has been a busy month, as I had to prepare for (and attend) 4 exams, as well as creating curriculum for the <strong><em>introduction to python and numerical algorithms for solving differential equations</em></strong> used in this <a href="https://unf.dk/aktiviteter/2025-06-29/unf-matematisk-modelleringsuge/">UNF summer camp</a>, where I am volunteering.</p>

<p>So instead of rushing a project, now that exams are done, I thought it would be interesting to share my exam project from the DTU course, “<strong>Advanced Deep Learning in Computer Vision</strong>”. The project was done in collaboration with 2 awesome guys, Joakim Dihn and Vebjörn Skre, with the professor Aasa Feragen as supervisor (who also happened to supervise my Bachelor’s thesis)</p>

<h2 id="project-formulation"><a href="#project"></a>Project Formulation</h2>

<p>This project is about removing <em>shortcuts</em> from <a href="https://challenge.isic-archive.com/data/#2018">lesion images</a>. In classification, a “shortcut” is some kind of proxy in the image, that is highly correlated with a specific class, without being the actual object of interest. An example would be how a model that is supposed to distinguish between a <em>wolf</em> and a <em>husky</em> might look for snow in the background. This is because images of wolves are usually taken in cold places (usually with snow). This becomes a problem when an image of a wolf in either not in a snowy area, or we get an image of a husky that <em>is</em> in a snowy area:</p>

<p><img src="/MonthlyProjects/assets/images/shortcuts/wolf_husky.png" alt="wolf_husky" title="wolf_husky" /></p>

<p>In this project, the dataset consists of images of lesions of 7 classes (disease types + benign), but we are also provided with a label of whether the image contains a known shortcut or not. Shortcuts in this case are usually ruler markings on the skin.</p>

<p>Our goal is then to remove those shortcuts by first noising the image, and then recreate the image with the shortcut gone - esentially creating a <strong><em>counterfactual explanation</em></strong> of what an image would look like without the shortcut there. This is meant as a preprocessing step that can be applied to a dataset, such that the model that classifies lesion diseases will never be impacted by shortcuts, which lets the model focus on relevant features. The methods are based on <a href="https://arxiv.org/abs/2203.04306">this paper</a>.</p>

<h2 id="understanding-the-problem"><a href="#understand"></a>Understanding the Problem</h2>

<p>From the very start, we had a hard time really understanding the nuance of the proposed method. They put a lot of emphasis on the use of DDIM instead of DDPM. We thought that the only change here was to remove the variance of the standard DDPM denoising process. We now realize that the paper also proposed a deterministic <em>forward</em> process that let us “encode anatomical information”. The lack of stochasticity is good, since we are not interested in creating a more expressive image. The only source of stochasticity in the denoising process comes from how we guide the noise prediction toward the “non-shortcut” class.</p>

<p><img src="/MonthlyProjects/assets/images/shortcuts/algorithm1.png" alt="algorithm1" title="algorithm1" /></p>

<p>Notice in the forward process how the only “external noise” comes from the noise predictor. This makes it so that if no classifier guidance was present, we could reproduce the original image exactly through the denoising process. Speaking of classifier guidance, it took us some time to realize that using classifier guidance to guide the denoising toward the non-shortcut class was much better than training a diffusion model unconditionally (only training it on images without shortcuts). In both cases, the goal is to guide the denoising toward the distribution of images that don’t include shortcuts. But, with classifier guidance, we can tune the inference by not only using the noise level <em>L</em>, but also the gradient scale <em>s</em>.</p>

<h2 id="dataset-and-artificial-shortcuts"><a href="#dataset"></a>Dataset and <em>Artificial</em> Shortcuts</h2>

<p>To simplify the task at hand, we reduced the dataset to two classes:  “Melanoma diagnosis” and “Non-melanoma diagnosis”. Even though a “healthy” vs. “non-healthy” class would be more desirable; it just wasn’t an option. We wanted to see how much the shortcuts impacted predictions, but as the shortcuts themselves weren’t very clear, we introduced artificial green shortcuts that we would add to many “Melanoma” images. This was also done to increase the correlation between shortcuts and the melanoma class.</p>

<p>Removal of original shortcuts:
<img src="/MonthlyProjects/assets/images/shortcuts/denoise_orig.png" alt="denoise_orig" title="denoise_orig" /></p>

<p>Removal of artificial shortcuts:
<img src="/MonthlyProjects/assets/images/shortcuts/denoise_artificial.png" alt="denoise_artificial" title="denoise_artificial" /></p>

<p>Although not perfect, the impact of removing an artificial shortcut is way more clear.</p>

<h2 id="classifier-results"><a href="#results"></a>Classifier Results</h2>

<p>To check what a model “sees” when it makes a prediction, we have trained a Visual Transformer model (ViT) that shows which areas have higher associated attention. The model has been trained on the dataset with artificial shortcuts. The following first shows an example of an image from the artificial shortcut dataset, and below it, an image with the shortcut removed through diffusion:</p>

<p><img src="/MonthlyProjects/assets/images/shortcuts/attention.png" alt="attention" title="attention" /></p>

<p>It is very obvious from this, that the model abuses the shortcut when it’s present. When it’s not present, it actually looks at the lesions. More specifically, it look at the edge of the lesion, which is also what professional doctors use when looking for signs of disease.</p>

<p>We have also tried to compare accuracy of models. In the first case, the models have been trained and tested on the artificial shortcut dataset, and in the second one, the models have been trained on the dataset with artificial shortcuts <em>removed</em> - through diffusion, and tested on images with artificial shortcuts:</p>

<p><img src="/MonthlyProjects/assets/images/shortcuts/accuracy.png" alt="accuracy" title="accuracy" /></p>

<p>There are two bars, indicating the predictive ability of the model on shortcut- and non-shortcut images, respectively.</p>

<p>The top plot seems to completely over-utilize the shortcuts, as they get almost perfect predictions when shortcuts are present. They also perform very poorly when a shortcut is then <em>not</em> present.</p>

<p>The bottom plot shows the effect of removing shortcuts altogether (but testing on images with shortcuts). The models don’t seems to perform significatly differently despite shortcuts being present. On top of that, the accuracy even increases for the images without shortcuts, as the model can now focus on the actual lesions.</p>

<p>It should be noted that an accuracy below 50% in a binary classification task is a bit weird, since we would then just flip the prediction to get a better accuracy. We are aware of the “bad quality” of the models. The important thing is, that the model has a better understanding of the classes. We do still get a higher accuracy than before.</p>

<h2 id="discussion-and-conclusion"><a href="#discussion"></a>Discussion and Conclusion</h2>
<p>I think the quality of shortcut removal is quite good, but not nearly as impressive as in the paper. As we didn’t utilize the deterministic forward process, our diffusion model altered too much of the images, despite using the correct denoising process. It might have helped to find an even better pair of parameters for the noise level (L=160) and gradient scale (s=100).</p>

<p>Despite that, the still managed to increase generalizability. The lesion classifiers then just had to be trained better, such that it would reach accuracies of &gt;50%</p>]]></content><author><name>Marcus Presutti</name><email>marcus.presutti.eu@gmail.com</email></author><summary type="html"><![CDATA[A summary of my exam project in the course "Advanced Deep Learning in Computer Vision"]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4000/MonthlyProjects/shortcuts/attention.png" /><media:content medium="image" url="http://localhost:4000/MonthlyProjects/shortcuts/attention.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Historic projects</title><link href="http://localhost:4000/MonthlyProjects/A-historic" rel="alternate" type="text/html" title="Historic projects" /><published>2025-04-01T00:00:00+02:00</published><updated>2025-04-01T00:00:00+02:00</updated><id>http://localhost:4000/MonthlyProjects/A-historic</id><content type="html" xml:base="http://localhost:4000/MonthlyProjects/A-historic"><![CDATA[<h3 id="️-tron-game-2024">🏍️ Tron game [2024]</h3>
<p>I had gone a bit back and forth regarding game development, but I thought I’d give it a shot again, with the hope of creating an AI to play a custom game after being inspired by <a href="https://www.youtube.com/watch?v=AaGK-fj-BAM&amp;ab_channel=TheCodingTrain">this video</a> of a guy coding snake from the bottom up in JavaScript. I tried to recreate the old arcade classic <a href="https://tron.fandom.com/wiki/TRON_(arcade_game)">Tron</a></p>

<p><img src="/MonthlyProjects/assets/images/historic/tron1.png" alt="tron1" title="tron1" />
<img src="/MonthlyProjects/assets/images/historic/tron2.png" alt="tron2" title="tron2" /></p>

<h3 id="-bachelor-project-2023">🧑‍🎓 Bachelor project [2023]</h3>
<p>For my bachelor’s in <em>Artificial Intelligence and Data</em> I did a project in collaboration with the company I worked at, <a href="https://chemometec.com/">ChemoMetec</a>:</p>

<p><strong><a href="https://drive.google.com/file/d/143BYXhBwRpPDs7hQRv_YjQCr17BYUJiH/view?usp=sharing">Bayesian Optimization for Quality Improvement in Ultrasonic Welding</a></strong></p>

<p>To keep it short, the main objective was to tune parameters of a physical machine; the ultra-sound welder, but treating them as if they were simply model parameters in a machine learning model. I developed a heuristic for capturing a kind of <em>purity</em> of the welding by taking images of the resulting product and doing calculating scores based on these.</p>

<p>I did <code class="language-plaintext highlighter-rouge">Bayesian Optimization</code> using <code class="language-plaintext highlighter-rouge">Gaussian processes</code> to choose parameters, and then trained an <code class="language-plaintext highlighter-rouge">XGBoost</code> model to match features with certain purity scores. To add explainability to the model, I used <a href="https://shap.readthedocs.io/en/latest/">SHAP</a></p>

<p><img src="/MonthlyProjects/assets/images/historic/shap.png" alt="shap" title="shap" /></p>

<h3 id="-netto-store-ads-notification-2020">🛒 Netto store ads notification [2020]</h3>
<p>When I moved away from home, I always checked the Netto ads for good price drops, but this quickly felt like a daunting manual task. Browsing the ads required me to open the official Netto app, and then scroll through everything. I look for certain items, so everything else is just a distraction. I was thinking to myself that this must be automated.</p>

<p>I started by trying to use the official <a href="https://developer.sallinggroup.com/api-reference">Salling API</a> to extract the prices of goods in Netto, but it didn’t work the way I wanted. Instead, I ended up webscraping a site that hosted the store ads for all major convenience stores. Then, I tried using simple key-search to extract the items I was interested in, and then had it send an email to me with both the images and prices of the items in mind. This way I could check the mail when the ads were updated, and I only saw the items that made sense.</p>

<h3 id="-police-reports-2019">🚔 Police reports [2019]</h3>
<p>The final project of my programming class in high school has no limitation - we could built whatever we wanted. This happened to be my first “data analysis” project, and inspired me to pursue a career in data science (and machine learning). Alongside my friend, we built a website hosted using <a href="https://www.heroku.com/">Heroku</a>, that let us host a small website for free and includes an SQL database. The site was programmed in Python using <a href="https://www.djangoproject.com/">Django</a>.</p>

<p>The main content of the website was a map of Nordsjælland that mapped the severity of crimes in different municipalities, using 1 year of scraped data from the Danish police’s <a href="https://politi.dk/aktuelt/doegnrapporter">daily reports</a>. We basically did keyword search of different categories such as “violence”, “vehicle”, “stealing”, etc. We could map the incidences to different cities/municipalities indicated by a coordinate for the center, as well as marking that area by a circle based on population density/size. We used the <a href="https://www.crummy.com/software/BeautifulSoup/">BeautifulSoup4 (bs4)</a> package for scraping and <a href="https://leafletjs.com/">leaflet.js</a> as our open-source library for displaying the interactive map on the website.</p>

<h3 id="-color-ohms-converter2019">🔌 Color-ohms converter[2019]</h3>
<p>During my 3rd year of high school, I had a class in electronics. Especially, we started working with <em>analog</em> electronics, which requires us to build “breadboards” consisting of different components, most notably the <strong><em>resistor</em></strong>, which (to over-simplify) regulates the voltage going through a system.</p>

<p>Well, in order to know the <em>impedence</em> of a resistor, you’d have to read the colored labels on it:</p>

<p><img src="/MonthlyProjects/assets/images/historic/resistor.png" alt="resistor" title="Title" /></p>

<p>The main idea is that the order of the first colors indicate the significant digits, the following indicate the scale, and the later ones indicate tolerance and other less important attributes. As you can imagine, this is very tedious to do every time you want to find a certain resistor.
So that’s why I created a GUI that would help me convert colors to Impedence values, and vice versa. I used <code class="language-plaintext highlighter-rouge">PyQt4</code> for the GUI, and had a class representation of each resistor, which is an arbitrary choice.</p>

<h3 id="-discord-bots-2018">🤖 Discord bots [2018]</h3>
<p>The communications platform <a href="https://discord.com/">Discord</a> had become very famous, especially among gamers as myself. This could be attributed to the high quality interface, but maybe also the fact that you could build or import bots that could interact with the users. These could be music bots that played music in the voice channel, or game bots that let you play in the the general chat room. More generally, you can interact with a program using text commannds, which would allow people to actviate basically any function defineded in Pytyhon or JavaScript (or whichever languages are available). For me at least, it felt natural to try building on of my own, by following the official <a href="https://discord.com/developers/docs/intro">Discord developer documention</a>.</p>

<p>The first bot was meant to host a <a href="https://skribbl.io/">skribbl.io</a> game. It basically starts a lobby of this online party game by calling a command in the chat: <code class="language-plaintext highlighter-rouge">start skribbl</code> or something similar. It would then output the invite link with game settings already in place.</p>

<p>The next bot was made as a quick hackathon-like challenge in about 30 minutes. Upon request in the chat (<code class="language-plaintext highlighter-rouge">&lt;THEME&gt; &lt;@user&gt; &lt;AMOUNT&gt;</code>), it would go to a specific website and extract all relevant image links and send a ton of these as spam to another user in the discord server. Image links are rendered as images in discord, so this was a ton of fun to use to annoy each other.</p>

<h3 id="️-js-canvas-game-2018">🕹️ JS canvas game [2018]</h3>
<p>For my first class in programming in high school, we had to create a simple JavaScript canvas game. The game was about moving a character left and right in order to avoid missiles raining down from above. The code was super messy, and I felt like the project had potential, so I ended up re-programming the game a year later, as I had become more familar how to code <em>properly</em>. I ended up adding a speed boost and other special buttons, as well as upgrading the sprites.</p>

<p>These extra upgrades taught me about control-mechanics and how seemingly simple things have to be accounted for; such as clicking left and right at the same time, square vs. circular bounding boxes etc.</p>]]></content><author><name>Marcus Presutti</name><email>marcus.presutti.eu@gmail.com</email></author><summary type="html"><![CDATA[A brief description of some previous projects]]></summary></entry><entry><title type="html">0. Website (this one)</title><link href="http://localhost:4000/MonthlyProjects/B-website" rel="alternate" type="text/html" title="0. Website (this one)" /><published>2025-04-01T00:00:00+02:00</published><updated>2025-04-01T00:00:00+02:00</updated><id>http://localhost:4000/MonthlyProjects/B-website</id><content type="html" xml:base="http://localhost:4000/MonthlyProjects/B-website"><![CDATA[<h2 id="prologue"><a href="#prologue"></a>Prologue</h2>
<p>I really love coding. Coding is both my profession and my hobby, but unfortunately, I haven’t found time to commit to any interesting hobby projects as of late. My excuse is usually that I don’t have time, or that the project becomes to large and messy. Also, despite having done a ton of projects, none of them are really easy to showcase.</p>

<p>To fix all of these issues, I’ll try a <em>new approach</em>:</p>
<blockquote>
  <p><strong><em>“I have to complete and document at least 1 coding project each month”</em></strong>.</p>
</blockquote>

<p>This way, I must start and finish a project, and then figure out the size of it, depending on how much time I have available. In reality there is never a good moment to start a project - <em>so just do it</em>.</p>

<h2 id="setting-up-a-jekyll-website"><a href="#website"></a>Setting up a Jekyll website</h2>
<p>Enough rambling - now to the fun part!</p>

<p>To kick this off, it makes a ton of sense to start by creating a website to document all the monthly projects. I wanted to create a blog-like website, where each project would be covered in one post. So far, the easiest way for me to host a website, has been to do it through <a href="https://pages.github.com/">GitHub Pages</a>. GitHub let’s you host a <a href="https://jekyllrb.com/">Jekyll</a> website completely for free. Jekyll sites are easy to set up, and don’t require any database connections. This also means that all the content (source files and blog post content) is all gathered in a single repository, which works fine for me.</p>

<p>To get started, I followed <a href="https://github.com/sfarrens/jekyll_tutorial">this guide</a> on setting up Jekyll (and Ruby, which it runs on). 
The highlights consist of:</p>

<ol>
  <li><a href="https://jekyllrb.com/docs/installation/ubuntu">Installing Ruby and other prerequisites for Ubuntu</a>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get <span class="nb">install </span>ruby-full build-essential zlib1g-dev
</code></pre></div>    </div>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">echo</span> <span class="s1">'# Install Ruby Gems to ~/gems'</span> <span class="o">&gt;&gt;</span> ~/.bashrc
<span class="nb">echo</span> <span class="s1">'export GEM_HOME="$HOME/gems"'</span> <span class="o">&gt;&gt;</span> ~/.bashrc
<span class="nb">echo</span> <span class="s1">'export PATH="$HOME/gems/bin:$PATH"'</span> <span class="o">&gt;&gt;</span> ~/.bashrc
<span class="nb">source</span> ~/.bashrc
</code></pre></div>    </div>
  </li>
  <li>Install Jekyll
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gem <span class="nb">install </span>jekyll bundler
</code></pre></div>    </div>
  </li>
  <li>Create the Jekyll folder structure
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>jekyll new <span class="nb">.</span> <span class="nt">--force</span>
</code></pre></div>    </div>
  </li>
  <li>Launching the server locally
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>bundle <span class="nb">exec </span>jekyll serve
</code></pre></div>    </div>
  </li>
</ol>

<h2 id="adjusting-the-theme"><a href="#theme"></a>Adjusting the Theme</h2>
<p>So, the default <code class="language-plaintext highlighter-rouge">minima</code> theme didn’t resonate with me. Luckily, Jekyll let’s one install new official themes easily by adding the <code class="language-plaintext highlighter-rouge">theme: &lt;THEME_NAME&gt;</code> to the <code class="language-plaintext highlighter-rouge">_config.yml</code> file, adding <code class="language-plaintext highlighter-rouge">gem &lt;THEME_NAME&gt;</code> to the <code class="language-plaintext highlighter-rouge">Gemfile</code>, and then <em>update the bundle</em> using:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>bundle update
</code></pre></div></div>
<p>Unfortunately, the I preferred, somehow was not compatible with Jekyll v4, which is required for it to run on GitHub pages. Therefore I had to implement it manually from the theme’s <a href="https://github.com/tocttou/hacker-blog">source code</a>, which itself is a fork of the original <a href="https://pages-themes.github.io/hacker/">hacker theme</a>. It was surprisingly easy to get to work, so it was definitely worth the detour. From this point, it was straight-forward to implement small tweaks such as scrollbar color, fixed header, month emojis, etc.</p>

<p>I will admit that it required some more knowledge of the Jekyll structure to get right - so I had to go through the official <a href="https://jekyllrb.com/docs/step-by-step/01-setup/">step-by-step tutorial</a> at that point. But as you can see, the website ended up quite nice - if I may say so myself.</p>]]></content><author><name>Marcus Presutti</name><email>marcus.presutti.eu@gmail.com</email></author><summary type="html"><![CDATA[First project in a series of monthly projects. This requires a website ;)]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4000/MonthlyProjects/all/nyancat.gif" /><media:content medium="image" url="http://localhost:4000/MonthlyProjects/all/nyancat.gif" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">1. Textual Inversion</title><link href="http://localhost:4000/MonthlyProjects/C-textual_inversion" rel="alternate" type="text/html" title="1. Textual Inversion" /><published>2025-04-01T00:00:00+02:00</published><updated>2025-04-01T00:00:00+02:00</updated><id>http://localhost:4000/MonthlyProjects/C-textual_inversion</id><content type="html" xml:base="http://localhost:4000/MonthlyProjects/C-textual_inversion"><![CDATA[<h2 id="prologue"><a href="#prologue"></a>Prologue</h2>
<p>With the website quickly done, I still had time for another April project. I am currently taking a course on deep learning in computer vision, where we had about diffusion models for some weeks. A few weeks ago, I was not able to do the task on <em>textual inversion</em>. We were given most of the code, but the code stack was kind of a mess to get an overview of, and as a result, I could not generate anything remotely interesting. I was thinking to myself that it was not supposed to be that hard to implement, which is why I went to the internet. And as I should have thought, I was lead to <a href="https://huggingface.co">Hugging Face 🤗</a></p>

<h2 id="textual-inversion"><a href="#textual-inversion"></a>Textual Inversion</h2>
<p>Let’s start from scratch: “What is textual inversion?”</p>

<p>So, diffusion models like “Stable Diffusion” use (most notably) text to generate images. In order for these models to use that for anything, we need a model that learns comparisons between training images and their respective text labels. For this, a common solution is to use the CLIP model. This model encodes text and images in pairs, into a latent space, where the model tries to learn the encoding that increases similarity between image and text in each pair. Each <strong>&lt;token&gt;</strong> now has it’s own latent representation that, through the prompt, guides how the diffusion model should produce an image.</p>

<p>But what if I want to create an image of something very specific that can’t be captured my a prompt? Do we then need to retrain the entire diffusion model? Luckily no, since the diffusion model and CLIP model are completely independent of each other. Instead, we use <strong>textual inversion</strong> to create a new embedding, that belongs to the same latent space that the CLIP encoder captures. This requires us to use a few images (5-7 should be plenty) that belongs to that embedding, in order to start.</p>

<p>To make it easy for ourselves, we can initialize a new embedding from an existing similar one. Then, we update the embedding directly, by optimizing wrt. the predicted noise of the diffusion model, similarly to how we’d normally train a diffusion model. This entire process should be done while freezing all parameters that are not the embedding.</p>

<p>This was a very brief overview. For more information, visit the original paper: <a href="https://arxiv.org/abs/2208.01618">An Image is Worth One Word: Personalizing Text-to-Image Generation using Textual Inversion
</a></p>

<p><img src="/MonthlyProjects/assets/images/textual_inversion/textual_inversion_paper.png" alt="textualinversion" title="textualinversion" /></p>

<blockquote>
  <p>TLDR: I have some specific images of an object or style, and I want my diffusion model to learn to represent that. This is done by teaching the model a new embedding vector.</p>
</blockquote>

<p>Alright now, let’s get to work!</p>

<h2 id="hugging-face-"><a href="#hugging-face"></a>Hugging Face 🤗</h2>

<p>The journey starts at a <a href="https://huggingface.co/docs/diffusers/training/text_inversion">Hugging Face tutorial</a>, which forms the base of this (rather small) project. All the “logic” has been provided, so we only need to concern ourselves with data, training parameters, and inference. This is the only things we need to define before running the training code:</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">os</span>
<span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">[</span><span class="s">"MODEL_NAME"</span><span class="p">]</span> <span class="o">=</span> <span class="s">"stable-diffusion-v1-5/stable-diffusion-v1-5"</span>
<span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">[</span><span class="s">"DATA_DIR"</span><span class="p">]</span> <span class="o">=</span> <span class="s">"./charizard"</span>
<span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">[</span><span class="s">"HUGGINGFACE_HUB_TOKEN"</span><span class="p">]</span> <span class="o">=</span> <span class="o">&lt;</span><span class="n">HUGGINGFACE_HUB_TOKEN</span><span class="o">&gt;</span>
</code></pre></div></div>

<p>The downside of relying solely on Hugging Face’s implementation, is that it only works for the simpler Stable-Diffusion v1.5 models, and not the more impressive v3.5 models. This is not because of model complexity per se, but rather that the newer models don’t use CLIP (or in the same way at least)</p>

<h2 id="dataset"><a href="#dataset"></a>Dataset</h2>

<p>In the tutorial, we are creating an embedding of a weird cat-like statue:
<img src="/MonthlyProjects/assets/images/textual_inversion/cat.jpeg" alt="cat" title="cat" /></p>

<p>But I have a better idea: Let’s instead create an embedding for the famous Pokémon <strong>Charizard</strong>:
<img src="/MonthlyProjects/assets/images/textual_inversion/charizard_base.jpg" alt="charizard_base" title="charizard_base" /></p>

<p>It was very straight forward to create a <a href="https://huggingface.co/docs/diffusers/training/create_dataset">custom datasets</a> instead, as it simply required one to have a folder with images in, and then pass the folder name as a training argument.</p>

<p>I used a collection of Pokémon images from a <a href="https://www.kaggle.com/datasets/thedagger/pokemon-generation-one">Kaggle dataset</a>. It contains a total of 52 images of Charizard (as well as all other Pokémon), but not all of the images are of great quality. I picked the 7 cleanest ones for this project. 
<img src="/MonthlyProjects/assets/images/textual_inversion/charizard_all.png" alt="charizard_all" title="charizard_all" /></p>

<h2 id="training"><a href="#training"></a>Training</h2>

<p>With most of the code given, and the images selected, the training really comes down to the choice of training arguments - all of which are defined in <a href="https://github.com/huggingface/diffusers/blob/main/examples/textual_inversion/textual_inversion.py">this script</a></p>

<p>I could tune on this all day, but this was the arguments I ended up with:</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">torch</span>
<span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="n">empty_cache</span><span class="p">()</span>

<span class="err">!</span><span class="n">accelerate</span> <span class="n">launch</span> <span class="n">textual_inversion</span><span class="p">.</span><span class="n">py</span> \
  <span class="o">--</span><span class="n">pretrained_model_name_or_path</span>    <span class="o">=</span>   <span class="err">$</span><span class="n">MODEL_NAME</span> \
  <span class="o">--</span><span class="n">train_data_dir</span>                   <span class="o">=</span>   <span class="err">$</span><span class="n">DATA_DIR</span> \
  <span class="o">--</span><span class="n">output_dir</span>                       <span class="o">=</span>   <span class="s">"textual_inversion_charizard"</span> \
  <span class="o">--</span><span class="n">learnable_property</span>               <span class="o">=</span>   <span class="s">"object"</span> \
  <span class="o">--</span><span class="n">report_to</span>                        <span class="o">=</span>   <span class="s">"wandb"</span> \

  <span class="o">--</span><span class="n">placeholder_token</span>                <span class="o">=</span>   <span class="s">"&lt;charizard&gt;"</span> \
  <span class="o">--</span><span class="n">initializer_token</span>                <span class="o">=</span>   <span class="s">"dragon"</span> \

  <span class="o">--</span><span class="n">resolution</span>                       <span class="o">=</span>   <span class="mi">512</span> \
  <span class="o">--</span><span class="n">train_batch_size</span>                 <span class="o">=</span>   <span class="mi">1</span> \
  <span class="o">--</span><span class="n">gradient_accumulation_steps</span>      <span class="o">=</span>   <span class="mi">4</span> \

  <span class="o">--</span><span class="n">gradient_checkpointing</span>           <span class="o">=</span>   <span class="bp">True</span> \
  <span class="o">--</span><span class="n">mixed_precision</span>                  <span class="o">=</span>   <span class="s">"fp16"</span> \
  <span class="o">--</span><span class="n">enable_xformers_memory_efficient_attention</span> \

  <span class="o">--</span><span class="n">max_train_steps</span>                  <span class="o">=</span>   <span class="mi">2000</span> \
  <span class="o">--</span><span class="n">learning_rate</span>                    <span class="o">=</span>   <span class="mf">1e-2</span> \
  <span class="o">--</span><span class="n">lr_warmup_steps</span>                  <span class="o">=</span>   <span class="mi">100</span> \
  <span class="o">--</span><span class="n">scale_lr</span>                         <span class="o">=</span>   <span class="bp">True</span> \
  <span class="o">--</span><span class="n">lr_scheduler</span>                     <span class="o">=</span>   <span class="s">"cosine"</span> 
</code></pre></div></div>
<p>Small comments on this: I use “weights and biases” (<a href="https://wandb.ai/home">wandb</a>) to track losses live during training. Also note that we can choose to train either an “object” or “style” embedding.</p>

<p>Even though the training process is dependent on many parameters intertwined, I’d say we can still group tuning into at least these few categories:</p>
<ol>
  <li>Convergence</li>
  <li>Memory efficiency</li>
  <li>Architecture  (but this is out of scope for this project)</li>
</ol>

<h3 id="convergence"><a href="#convergence"></a>Convergence</h3>
<p>I chose to initialize the embedding as a “dragon”, as “Charizard” is also a dragon - even though it’s a Pokémon as well. As “Charizard” is just an animated character, it doesn’t fully represent a dragon as we know it from fantasy. To see what dragons might look like with SD v1.5 model, consider the following examples:</p>

<p><img src="/MonthlyProjects/assets/images/textual_inversion/normal_dragon.png" alt="normal_dragon" title="normal_dragon" /></p>

<p>Despite tuning a lot with learning-rate, learning-rate scheduling, warmup steps, and learning-rate scaling, I usually get loss curves like this:</p>

<p><img src="/MonthlyProjects/assets/images/textual_inversion/loss.png" alt="loss_curve" title="loss_curve" /></p>

<p>This shows absolutely no sign of learning, even though, the embedding is definitely better than the initialization, or at least closer to a “Charizard” (which should be evident from the results)</p>

<h3 id="memory"><a href="#memory"></a>Memory</h3>
<p>As always when working with deep learning on image data, memory quickly becomes an issue, as we usually want fast training + high resolution images. Getting most out of one’s GPU is not a trivial task; therefore we need to utilize as many optimization tricks as possible. But first, let’s check out the stats of the GPU we are using.</p>

<p>Unless you are a gamer, it is unlikely that you own a modern NVIDIA GPU that isi suitable for training DL models. Google Colab therefore becomes a good first choice for (cloud) training. But I actually have a GPU locally, meaning that I should consider which GPU (and setup) I prefer.</p>

<p>Let’s start by considering the GPU specs. Free tier users on Colab get a <strong>Tesla T4</strong> GPU, and I own a <strong>GeForce RTX 2070 Super</strong>:</p>

<table>
  <thead>
    <tr>
      <th> </th>
      <th><strong>Tesla T4</strong></th>
      <th><strong>GeForce RTX 2070 Super</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>GPU Memory (VRAM)</td>
      <td><strong><em>16 GB</em></strong></td>
      <td>8 GB</td>
    </tr>
    <tr>
      <td>FP16/FP32 Compute Performance</td>
      <td>8.141 TFLOPS</td>
      <td><strong><em>9.062 TFLOPS</em></strong></td>
    </tr>
    <tr>
      <td>Memory Bandwidth</td>
      <td>320.0 GB/s</td>
      <td><strong><em>448.0 GB/s</em></strong></td>
    </tr>
    <tr>
      <td>Power consumption (TDP)</td>
      <td><strong><em>70 Watt</em></strong></td>
      <td>215 Watt</td>
    </tr>
    <tr>
      <td>CUDA Cores</td>
      <td>2560</td>
      <td>2560</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Stats are taken from <a href="https://technical.city/en/video/Tesla-T4-vs-GeForce-RTX-2070-Super">here</a></p>
</blockquote>

<p>The Tesla GPU provides double as much VRAM, which is awesome, but the RTX 2070 GPU is faster. 
Another thing is that when working on Colab, you are assigned a “session”, which you can be disconnected from. On the other hand, running training locally means that the PC is noisy, and can’t run other GPU heavy tasks. In conclusion, Colab might have a better GPU in my opinion, as I value VRAM a lot. Despite that, I think the ease of running things locally is what works best for me, so <em>I opted with running the training locally</em>.</p>

<p>With the limited VRAM, I have to be extra cautious about memory use. I need some tricks!</p>

<h4 id="mixed-precision"><a href="#mixed-precision"></a>Mixed precision</h4>
<p>In order to update weights and gradients with high precision, 32-bit precision is usually used ±3.4<em>x</em>10^38. To reduce the amount of memory for storing these large number, we use <em>mixed precision</em>. This means that for certain calculations and weights, only 16-bit precision is used ±6.5<em>x</em>10^4. We risk losing some precision, but get a significant memory increase in return, and usually also a speedup with certain optimized hardware.</p>

<h4 id="gradient-checkpointing"><a href="#gradient-checkpointing"></a>Gradient Checkpointing</h4>
<p>As I understand it, a lot of memory is used during backpropagation because we store all activations. Therefore it is sometimes preferable to only store some of the activations, and recompute the others. This obviously requries more compute time, but it saves a lot of memory as well.</p>

<h4 id="xformers"><a href="#xFormer"></a>xFormers</h4>
<p>Transformers are present many places throughout stable diffusion, and by using <em>xFormers</em> we get almost risk-free optimizations, like reduced VRAM and faster computation. Even though we don’t train the transformers themselves, they are still used in training.</p>

<h4 id="gradient-accumulation"><a href="#gradient-accumulation"></a>Gradient accumulation</h4>
<p>Despite all the optimization tricks, I was not able to create a 512x512 image, using a batch-size of more than 1. I was not willing to reduce the image resolution, so I figured out an amazing trick, that I have already used for other DL projects: <em>gradient accumulation</em>. When increasing the batch-size, much more data is contained in memory. This is what allows models to utilize the parallelization ability of the GPU.</p>

<p>While mini-batching lets you compute gradients over all element in the batch, gradient accumulation computes the gradient over multiple batches. So, when you can’t fit a larger batch-size into memory, but still want a better gradient estimate, you can use the next batch(es) as a part of the estimate, before updating weights. In this context, <em>effective</em> batch size is the total number of samples that went into a gradient estimate:</p>
<pre><code class="language-math">eff_batch_size = batch_size * gradient_accumulation_steps
</code></pre>
<p>So clarify, gradient accumulation kind of gives you are larger batch-size, but the accurate expression is that the “effective batch size” increases.</p>

<p>While batches can be processed in parallel, gradient accumulation requires a full forward+backward pass for each gradient accumulation step.</p>

<h4 id="deepspeed"><a href="#deepspeed"></a>DeepSpeed</h4>
<p>DeepSpeed is an open-source library my Microsoft that can add significant performance boosts to model training… if you utilize <em>multiple</em> GPUs. As neither Colab nor my local setup has this capability, adding DeepSpeed doesn’t really add anything. It might have something especially for single GPU use, but it did not seem to have an actual impact on my model.</p>

<h2 id="results"><a href="#results"></a>Results</h2>
<p>Now that the model has been trained, the only thing left is to create some prompts, and generate some images. The inference code looks something like this:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">diffusers</span> <span class="kn">import</span> <span class="n">StableDiffusionPipeline</span>

<span class="c1"># Load the base model
</span><span class="n">pipeline</span> <span class="o">=</span> <span class="n">StableDiffusionPipeline</span><span class="p">.</span><span class="n">from_pretrained</span><span class="p">(</span>
    <span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">[</span><span class="s">"MODEL_NAME"</span><span class="p">],</span> 
    <span class="n">torch_dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">float16</span>
<span class="p">).</span><span class="n">to</span><span class="p">(</span><span class="s">"cuda"</span><span class="p">)</span>

<span class="c1"># Load your custom textual inversion embedding
</span><span class="n">pipeline</span><span class="p">.</span><span class="n">load_textual_inversion</span><span class="p">(</span><span class="s">"textual_inversion_charizard"</span><span class="p">,</span> <span class="n">placeholder_token</span><span class="o">=</span><span class="s">"&lt;charizard&gt;"</span><span class="p">)</span>

<span class="c1"># Prompt model
</span><span class="n">prompt</span> <span class="o">=</span> <span class="s">"Something ... &lt;charizard&gt; ... something"</span>
<span class="n">image</span> <span class="o">=</span> <span class="n">pipeline</span><span class="p">(</span><span class="n">prompt</span><span class="p">,</span> <span class="n">num_inference_steps</span><span class="o">=</span><span class="mi">50</span><span class="p">).</span><span class="n">images</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>
<span class="n">image</span><span class="p">.</span><span class="n">save</span><span class="p">(</span><span class="s">"out.png"</span><span class="p">)</span>
</code></pre></div></div>

<p>The strategy was quite simple. In my experience, some keywords/tags seem to be more effective than others, which is why I used ChatGPT to craft a diffusion prompt from natural language. Tags like “8K” and “ArtStation trending” seem to be associated with higher quality images. I assume that newer models have natural language capabilities incorporated - but that is not the case the early stable diffusion models.</p>

<p>My first attempt at a prompt was to create a more or less realistic looking Charizard, which the model interpreted as smooth 3D-like:</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">prompt</span> <span class="o">=</span> <span class="s">"A hyper-realistic cinematic illustration of &lt;charizard&gt; soaring through a dramatic sky, glowing embers around it, wings spread wide, powerful fire breath, epic lighting, golden hour, volumetric light, ultra-detailed, 4k, concept art, artstation, trending on ArtStation"</span>
</code></pre></div></div>
<p><img src="/MonthlyProjects/assets/images/textual_inversion/hyper_realistic.png" alt="hyper_realistic" title="hyper_realistic" /></p>

<p>I then realized that the training images were all of a cartoon style, so my intuition was now to create a more cartoony look:</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">prompt</span> <span class="o">=</span> <span class="s">"&lt;charizard&gt;, a dragon-like Pokémon with blazing orange scales, roaring as it flies through a stormy sky. Flames burst from its mouth, lighting a volcanic land with lava and obsidian cliffs. Hyper-detailed digital art, vibrant colors, cinematic lighting, realism meets anime. ArtStation trending, 8K, dramatic scene with smoke swirling around its fiery tail"</span>
</code></pre></div></div>
<p><img src="/MonthlyProjects/assets/images/textual_inversion/dragon_like.png" alt="dragon_like" title="dragon_like" /></p>

<p><img src="/MonthlyProjects/assets/images/textual_inversion/majestic_dragon.png" alt="majestic_dragon" title="majestic_dragon" /></p>

<p>The results were interesting but not impressive. I was then unsure if the training had gone wrong, or if the model is simply very bad. I then tried the following prompt, where I switched out “dragon” with “&lt;charizard&gt;” in one of the 3 cases. You can probably guess which.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">prompt</span> <span class="o">=</span> <span class="s">"A breathtaking, ultra-detailed cartoon illustration of dragon, majestic and powerful, flying through a dramatic sunset sky, vibrant and saturated colors, intricate fire and smoke effects, cinematic lighting, highly polished fantasy art, masterpiece, 8k, extremely sharp and clean linework, professional character design"</span>
</code></pre></div></div>
<p><img src="/MonthlyProjects/assets/images/textual_inversion/ultra_detailed.png" alt="ultra_detailed" title="ultra_detailed" /></p>

<p>Just for comparison, the following images are from using the prompts “Charizard” and “Charizard cartoon” on the stable diffusion v3.5 large model without textual inversion:
<img src="/MonthlyProjects/assets/images/textual_inversion/sd35.png" alt="sd35" title="sd35" /></p>

<h2 id="reflection"><a href="#reflection"></a>Reflection</h2>
<p>While not the quality I had hoped for, it was still fun to work with training optimizations, and especially to observe the monstrosities that the model created. While the simple SD v1.5 is quite small/simple, it is very accesible, and should also be quite simple to fine-tune using LoRA (which I look forward to in the future).</p>

<p>I am not entirely satisfied with my efforts in lowering the training loss. I should have been more meticulous in tracking losses from the start, and tuning more responsively. The Charizard embedding could have been of much higher quality.</p>

<p>Now that I have become slightly more accustomed to the Huggingface API, I think a next step in this domain would definitely be to try larger models, and to do some more <em>heavy</em> training.</p>]]></content><author><name>Marcus Presutti</name><email>marcus.presutti.eu@gmail.com</email></author><summary type="html"><![CDATA[Utilize textual inversion to learn an word-embedding of the Pokémon "Charizard", in order to generate an image with Stable Diffusion 1.5]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4000/MonthlyProjects/textual_inversion/hyper_realistic.png" /><media:content medium="image" url="http://localhost:4000/MonthlyProjects/textual_inversion/hyper_realistic.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry></feed>